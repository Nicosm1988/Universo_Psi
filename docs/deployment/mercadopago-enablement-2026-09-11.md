# Habilitación de Mercado Pago — evidencia del 11/09/2026

Estado vigente al 12/09/2026: **Mercado Pago habilitado en Production para PROFESSIONAL_MONTHLY por ARS 120.000/mes, con aprobación explícita del usuario**. El deployment **dpl_Gp5xuyMB42J7cXZitzN5ozPpvaVq**, READY, sirve el dominio canónico desde la comprobación de las **23:12:44 UTC**. La única correctiva quedó aplicada: postflight **10/10**, controles adicionales **8/8** y caché de RPC verificados. Cuenta real63533235 y app2533766701506518 corroboradas. La firma productiva pasó un probe **sintético sin cargo** sobre BET; el E2E sandbox verificó activación y cancelación automáticas. Production=true y Preview=false tienen registros separados; BET queda disponible como recuperación compatible y cerrada. **No se realizaron cargos reales ni cambios de precio**. La evidencia integrada está en [QA de pagos](mercadopago-qa-2026-09-11.md).

## Inventario histórico de recursos y acceso, previo al release

Las tablas iniciales conservan el diagnóstico previo a la apertura. Sus estados pendientes son históricos: el estado vigente está en el encabezado y en «Resultado por ambiente».

| Recurso | Evidencia histórica de esta sesión | Consecuencia observada entonces |
| --- | --- | --- |
| Repositorio | `Nicosm1988/Universo_Psi`, rama productiva `main`; base de trabajo y commit servido `d9347fa02714890f1258fe540635f63d5787a9d0` | El deployment actual todavía no contiene las correcciones locales. El release futuro debe identificar su commit definitivo y preservar los cambios existentes. |
| Vercel | CLI `whoami` funciona; proyecto y variables inspeccionados por el coordinador | El bloqueo inicial de autenticación quedó superado. Se publicó únicamente un Preview aislado; no se desplegó Production. |
| Proyecto Vercel | `universo-psi`, ID `prj_PWqxPQDfHR3eJDVQXO9nAfa96vR3`, equipo `team_zbDtU9hfwTxtJvcj9sJMOp9u`, slug `nmarcosan-2648s-projects`; Node `24.x` | Destino verificado y vínculo local `.vercel/project.json` correcto. |
| Deployment servido | `dpl_61kk8DxWRHVvC3apUe5F4TfnA2dZ`, `READY`; URL `https://universo-4g8rwwt1n-nmarcosan-2648s-projects.vercel.app`; alias `https://universo-psi-eight.vercel.app` | `READY` y el shell público no prueban operación de pagos ni precio vigente. |
| Supabase configurado en Production | `https://gwizdgboqwpzyiaqcxbb.supabase.co`; tras autorizar la cuenta correcta, GET Management de proyecto y Auth responde **200**. Estado **ACTIVE_HEALTHY**, organización `qwxmgztzoidyzkelupdr`, región `ca-central-1`, creado el 30/08/2026 | Acceso administrativo de lectura resuelto. La restauración mostrada antes en el panel ya no es el estado vigente. Preflight de sólo lectura completado por datos; aplicar correctiva productiva sigue pendiente. `ACTIVE_HEALTHY` no demuestra pagos. |
| Panel Supabase aportado por el usuario | Captura del proyecto correcto con **Restoration in progress**, en una organización/cuenta distinta de la autorizada anteriormente | Evidencia histórica del navegador manual del usuario, separada de la comprobación posterior de Management `ACTIVE_HEALTHY`. La sesión no inició restauraciones. El coordinador completó autorización sólo en el perfil Supabase separado, sin conectar Chrome habitual. |
| Plan público Supabase | El 12/09/2026, aproximadamente 01:28 UTC, el coordinador consultó REST con la clave publicable configurada en Production, mantenida en memoria: respuesta **200**, `PROFESSIONAL_MONTHLY`, `is_active=true`, `pricing_status=PUBLISHED`, `price_amount=120000`, `currency=ARS`, `billing_interval=MONTH` | Confirma disponibilidad de REST y lectura de la tabla pública `plans`, además del plan mensual publicado. No demuestra migraciones aplicadas, salud completa, checkout ni pagos. Consulta de sólo lectura, sin PII. |
| Recursos Supabase heredados | La sesión anterior listaba `bdxwbklitnxdthwkygtb` (**red-senda**, `INACTIVE`) en `pjbhwhoclsiqqkcegjaq` y otro proyecto ajeno, `yhpjbetvxdiawostlkon` (**reinvension-profesional-prod**, `ACTIVE_HEALTHY`) | Ninguno es el destino configurado en Production. No restaurar ni migrar `red-senda` para resolver este despliegue. Su Auth apunta a `red-senda.vercel.app`. |
| Vínculo local Supabase | `supabase link --project-ref gwizdgboqwpzyiaqcxbb` terminó con exit 0, sin pedir contraseña. `project-ref` y `linked-project.json` coinciden con ese proyecto y organización `qwxmgztzoidyzkelupdr`; el pooler corresponde a la referencia nueva y no contiene contraseña | Vínculo heredado corregido. Metadata anterior respaldada de forma privada en `/tmp/psi-remote-config/supabase-link-before-gwiz`. No se ejecutaron `db push`, migraciones, `config push` ni escrituras remotas. |
| MCP Supabase | Tanto `supabase` como `supabase_local` devuelven `http://127.0.0.1:54321`; SQL devuelve `public.plans = NULL` | No son prueba de acceso al remoto ni de una base local de Universo Psi. Ese stack se preservó; QA usa el stack aislado propio de puerto 55321. |
| Mercado Pago | Production tiene nombres `MERCADOPAGO_PERSONAL_ACCESS_TOKEN`, `MERCADOPAGO_PERSONAL_PUBLIC_KEY`, `MERCADOPAGO_PERSONAL_WEBHOOK_SECRET` y `MERCADOPAGO_ACTIVE_ACCOUNT`, protegidos y no exportables (`[SENSITIVE]`) | Inventario remoto protegido; posteriormente root verificó cuenta real63533235 y app2533766701506518 mediante credencial local, sin reemplazar todavía esas variables. Webhooksecret local cargado, firma productiva pendiente. No se llamó a endpoints de creación de pagos reales. |

La existencia de herramientas, sesiones cacheadas o una URL pública no valida el acceso administrativo. No se registraron tokens, secretos, claves publicables ni datos personales en este documento.

Historia del diagnóstico: inicialmente Vercel devolvía `403` y faltaba el vínculo local; por eso sólo se conocía la referencia cacheada de Red Senda. El acceso posterior a Vercel resolvió esa ambigüedad y prevalece sobre el inventario inicial. Git registra el fork desde Red Senda y la separación de configuración el 29/08. La descarga automática de `.env.local` al vincular Vercel se retiró del repo y se guardó con acceso privado en `/tmp/psi-remote-config/development.env`, para evitar que configure por accidente el trabajo local.

## Producto aprobado y antecedentes comerciales

El alcance es únicamente **Suscripciones / Preapproval**, plan interno `PROFESSIONAL_MONTHLY`, mensual recurrente, **ARS 120.000 por mes**. La lectura REST del proyecto productivo del 12/09/2026 confirmó el plan activo y `PUBLISHED`, con importe 120000, moneda ARS y cadencia MONTH. Este importe no se modificó. La ausencia de compromiso de ciclos procede del modelo de negocio y la fuente local; no formó parte de esta consulta pública. Los otros planes continúan `DRAFT` según la fuente local y no necesitan precio para habilitar el mensual.

La cuenta receptora personal63533235 y la aplicación2533766701506518 se verificaron por API y quedaron incluidas en la aprobación del usuario. El snapshot productivo confirma ausencia de compromiso mínimo y gracia de3 días. No se inventan datos fiscales ni se toma el defaultpersonal como prueba de titularidad.

## Inventario histórico de variables antes de completar la configuración

Fuente de nombres: `src/lib/env/server.ts` y `.env.example`. La columna local describe el inventario inicial, sin contar fixtures de QA. Preview y Development estaban vacíos al recuperar acceso. La tabla siguiente corresponde al proyecto Vercel original; su Preview tiene configuración propia para el sandbox gratuito y Development permanece sin configuración de aplicación. El proyecto QA separado tiene su configuración vigente detallada más abajo (quince variables y checkout sandbox habilitado).

| Variable | Local inspeccionado entonces | Estado remoto observado entonces |
| --- | --- | --- |
| `NEXT_PUBLIC_SITE_URL` | Sin valor operativo local | Production: `https://universo-psi-eight.vercel.app`; Preview: `https://universo-psi-mp-sandbox.vercel.app`; Development: ausente |
| `NEXT_PUBLIC_SUPABASE_URL` | Sin valor operativo local | Production: proyecto `gwizdgboqwpzyiaqcxbb`; Preview: proyecto exclusivo `gzsbndvaxdyuyhjidfqq`; Development: ausente |
| `NEXT_PUBLIC_SUPABASE_PUBLISHABLE_KEY` | Ausente | Presente en Production y Preview, de proyectos distintos; Development: ausente |
| `SUPABASE_SECRET_KEY` | Ausente | Presente y protegida/no exportable en Production; Preview: clave propia del sandbox, protegida; Development: ausente |
| `RATE_LIMIT_SALT` | Sin configuración local verificada | Presente y protegida en Production; Preview: valor aleatorio propio protegido; Development: ausente |
| `CRON_SECRET` | Sin configuración local verificada | Production y Development: ausente; Preview: aleatorio propio protegido. Un valor presente no demuestra ejecución de cron |
| `MERCADOPAGO_ACTIVE_ACCOUNT` | Ausente; default del código `personal` | Presente y protegida en Production; valor no comprobado. Preview: personal; Development: ausente |
| `MERCADOPAGO_CHECKOUT_ENABLED` | Default `false` | Escritura exitosa y registro confirmado en Production y Preview; lectura de Preview confirmó exactamente `false`. Development: ausente. Configuración desplegada sólo en Preview; Production mantiene su versión anterior |
| `MERCADOPAGO_PERSONAL_ACCESS_TOKEN`, `MERCADOPAGO_PERSONAL_PUBLIC_KEY`, `MERCADOPAGO_PERSONAL_WEBHOOK_SECRET` | Ausentes | Presentes y protegidos/no exportables en Production; Preview/Development: ausentes |
| `MERCADOPAGO_PERSONAL_ENVIRONMENT`, `MERCADOPAGO_PERSONAL_COLLECTOR_ID` | Ausentes | Preview: ambiente sandbox; collector pendiente. Production/Development: ambos ausentes |
| `MERCADOPAGO_COMPANY_ACCESS_TOKEN`, `MERCADOPAGO_COMPANY_PUBLIC_KEY`, `MERCADOPAGO_COMPANY_WEBHOOK_SECRET` | Ausentes | Ausentes en los tres ambientes |
| `MERCADOPAGO_COMPANY_ENVIRONMENT`, `MERCADOPAGO_COMPANY_COLLECTOR_ID` | Ausentes | Ausentes en los tres ambientes |
| `UNIVERSO_PSI_TEST_MODE` | Sin configuración operativa local | Preview: `false` explícito; Production/Development: ausente, default del código nuevo `false` |

Los nombres históricos `MERCADOPAGO_ACCESS_TOKEN` y `MERCADOPAGO_WEBHOOK_SECRET` no sustituyen las variables por cuenta. `NEXT_PUBLIC_MERCADOPAGO_PUBLIC_KEY` permanece en el ejemplo histórico, pero no configura la credencial de servidor por cuenta. Nunca determinar sandbox/producción sólo por el prefijo del token; verificar aplicación, identidad del vendedor y evidencia autoritativa del recurso.

En la aplicación corregida, `MERCADOPAGO_CHECKOUT_ENABLED=false` cierra la creación y reanudación de checkouts, conservando la validación de webhooks y la recuperación administrativa con las credenciales históricas. **Agregar la variable en Vercel no detiene la versión ya desplegada:** requiere un nuevo deployment compatible. El interruptor tampoco detiene débitos que Mercado Pago ya tiene autorizados. Mantener `false` durante preparación; usar `true` para pruebas únicamente en un sandbox con cuenta e aislamiento verificados, o en Production después de aprobar la activación.

URLs derivadas de `NEXT_PUBLIC_SITE_URL`, por ambiente:

- Retorno recurrente: `/dashboard?subscription=checkout-return`.
- Webhook vendedor personal: `/api/webhooks/mercado-pago/personal`.
- Webhook vendedor empresa: `/api/webhooks/mercado-pago/company`.
- El producto futuro de pago único también contempla retornos `checkout-pending` y `checkout-failure`; no forma parte de la habilitación mensual.

Para Production el dominio y alias verificados son `https://universo-psi-eight.vercel.app`. Para sandbox se requiere una URL HTTPS pública estable del entorno aislado y webhook firmado accesible al proveedor. `localhost` o fixtures no prueban entrega externa. Asociar el `preapproval_plan_id` de sandbox sólo a su base y vendedor de pruebas; no copiarlo al mapeo productivo.

Correcciones locales preparadas por el coordinador: `next.config.ts` deriva el hostname de imágenes de `NEXT_PUBLIC_SUPABASE_URL` y usa el proyecto correcto como fallback; `supabase/config.toml` ya usa el dominio `universo-psi-eight.vercel.app` y callback correspondiente. No se ejecutó `config push` remoto. `.vercelignore` excluye credenciales, configuración MCP/agentes, artefactos de navegador y cachés; `vercel deploy --dry --json` comprobó 178 archivos, sin rutas excluidas y con fuentes/lockfile/migración requeridos presentes. Los ignores por defecto de Vercel se conservan.

## Configuración Supabase verificada tras recuperar acceso

GET Management `/config/auth` respondió 200: Site URL `https://universo-psi-eight.vercel.app`; allowlist `https://universo-psi-eight.vercel.app/**`; altas habilitadas, proveedor email habilitado y confirmación de correo requerida. Google está deshabilitado. La contraseña mínima remota es de 6 caracteres, no hay requisito adicional de caracteres, la reautenticación para cambiar contraseña está deshabilitada y CAPTCHA está deshabilitado. Estas diferencias con la configuración local requieren revisión dirigida; no ejecutar `config push` a ciegas ni ampliar redirects productivos para Preview.

GET `/v1/projects/gwizdgboqwpzyiaqcxbb/api-keys?reveal=false` respondió 200. Se inspeccionaron exclusivamente nombres/tipos: `anon` y `service_role` de tipo legacy; `default` de tipo publishable y `default` de tipo secret. La inspección inicial no solicitó revelar la secret key. Posteriormente el comando estándar de vinculación consultó internamente las claves y versiones de servicios para actualizar la caché local; no se imprimieron, exportaron a entornos ni persistieron valores de claves desde esta tarea. La [documentación oficial de Management API](https://supabase.com/docs/guides/integrations/supabase-for-platforms) permite leer claves existentes con `reveal=true` cuando sea necesario. Si se requiere preparar un entorno autorizado, consumir la respuesta únicamente en memoria y escribir directamente al gestor de secretos de su ambiente, sin chat, argumentos, logs ni archivos versionados. No crear o rotar claves para diagnosticar; no copiar la secret productiva a Preview.

Preview ya tiene base y credenciales propias, detalladas a continuación. El acceso administrativo al proyecto productivo permite inspeccionarlo; no convierte sus datos o credenciales en un entorno de prueba. El helper local privado de autenticación lee el llavero sin imprimir valores y fue entregado a `psi_datos_pagos` para preflight de sólo lectura.

## Sandbox gratuito y Preview preparados

Se verificó por Management API la organización `qwxmgztzoidyzkelupdr` en plan **free**, un único miembro Owner y un proyecto activo antes de la creación. El plan oficial [Free cuesta USD 0 al mes](https://supabase.com/pricing) y admite dos proyectos activos; el [cupo cuenta los proyectos de todos los Owners/Admins](https://supabase.com/docs/guides/platform/billing-faq). Con ese cupo y autorización coordinada se creó **universo-psi-mp-sandbox**, referencia **gzsbndvaxdyuyhjidfqq**, región **ca-central-1**, respuesta 201/ACTIVE_HEALTHY. Después de crearlo se volvió a verificar plan **free**. No se contrató un upgrade, branching ni addons.

La contraseña de la base y claves propias se generaron/obtuvieron únicamente para ese sandbox y se guardaron en archivos privados 0600. Preview recibió URL/clave publicable/secret del sandbox, `RATE_LIMIT_SALT` y `CRON_SECRET` nuevos, `UNIVERSO_PSI_TEST_MODE=false`, cuenta personal y ambiente sandbox. `MERCADOPAGO_CHECKOUT_ENABLED=false` permanece. Durante esa preparación inicial no se leyeron credenciales Mercado Pago; la validación posterior autorizada del vendedor se registra abajo. El repo sigue vinculado a Production; el trabajo de la base sandbox usa referencia explícita y directorio propio de `/tmp`.

Preview compatible **READY**: deployment **`dpl_8Zzp8krBbYavqef94ocghNNpw7c4`**, URL inmutable **https://universo-mdzm5eqkk-nmarcosan-2648s-projects.vercel.app**, alias asignado **https://universo-psi-mp-sandbox.vercel.app**. Pertenece al proyecto Vercel existente, pero usa exclusivamente claves/datos del sandbox nuevo y checkout cerrado. Auth del sandbox usa ese alias como Site URL, callback exacto `/auth/callback` y callback local `http://localhost:3179/auth/callback`. `psi_datos_pagos` completó las ocho migraciones legacy y fixtures mínimos ficticios, sin usuarios ni suscripciones al entregar; la aplicación compatible permite ensayar después la correctiva bajo coordinación, sin modificar Production.

**Protección y webhook:** el proyecto Vercel existente conserva SSO `all_except_custom_domains`. La revisión automática rechazó crear un bypass persistente para todos sus deployments por ampliar acceso más allá del alcance autorizado. Esa acción **no se ejecutó ni se reintentó**. Se preparó en cambio un [shareable link oficial](https://vercel.com/docs/deployment-protection/methods-to-bypass-deployment-protection/sharable-links) acotado al ID exacto del Preview y a 24 horas, autorizado por el coordinador. Su secreto y entrada de navegador se conservaron temporalmente en archivos privados; el token inicial se revocó y regeneró tras detectar que una respuesta de autenticación incluía el callback. El enlace se revocó y sus archivos de entrada/secreto se eliminaron después de comprobar el nuevo Preview público; no queda autorización de compartir vigente de esta sesión.

Smoke del enlace acotado: GET inicial establece una cookie y termina en la aplicación con HTTP 200; con esa cookie, POST al webhook llega al handler y devuelve 503 por configuración MP pendiente. **El POST externo sin cookie devuelve 307 de Vercel, por lo que este enlace no sirve como webhook directo de Mercado Pago.** Sin enlace/cookie, Vercel devuelve 401. Estos resultados prueban accesibilidad del Preview para revisión y alcance del handler, no un pago ni un webhook válido.

Alternativa más acotada **creada y autorizada para desplegar**: proyecto Vercel **`universo-psi-mp-test`**, ID **`prj_nZDkEEpIowd5rSJbYBWfyhQuVZDl`**, exclusivamente QA. La creación configuró Node 24/Next.js, sin Git conectado y `ssoProtection=null` únicamente en este proyecto nuevo. La inspección inicial confirmó **cero variables y cero deployments**; después el usuario autorizó explícitamente la transferencia y el despliegue. El proyecto original conserva `all_except_custom_domains`. El equipo sigue Hobby activo después de crear el proyecto. Antes se verificaron 29 proyectos; [Hobby es gratuito y admite 200 proyectos](https://vercel.com/docs/plans/hobby), sujeto a uso personal/no comercial y a sus límites gratuitos. No se solicitó upgrade, addon ni recurso pago.

La configuración lista para ese nuevo Preview usa exclusivamente Supabase `gzsbndvaxdyuyhjidfqq`, claves propias, `RATE_LIMIT_SALT`/`CRON_SECRET` aleatorios del sandbox, `MERCADOPAGO_CHECKOUT_ENABLED=false` y `UNIVERSO_PSI_TEST_MODE=false`. Site URL propuesta **https://universo-psi-mp-test.vercel.app**; deployment sólo Preview, variables sólo Preview y ningún secreto productivo o Mercado Pago. La raíz continúa vinculada al proyecto original; fuentes permitidas y vínculo nuevo están preparados en `/tmp/psi-vercel-mp-test`. Las variables permanecen privadas en `/tmp/psi-remote-config/sandbox.test-preview.env`, permisos 0600.

**Autorización posterior al bloqueo:** la revisión automática inicialmente rechazó transferir claves de Supabase y secretos de rate limit/cron por falta de autorización explícita para ese destino. Se detuvo sin reintentar. El usuario respondió después **«Sí, dale»** a cargar esas variables exclusivas de QA en Preview de `universo-psi-mp-test` y desplegar con checkout cerrado. Con esa nueva autorización se transfirieron diez variables sólo Preview; secretos de servidor como sensibles, sin MP ni Production. Auth de la base sandbox se ajustó al alias nuevo, conservando los callbacks anteriores de QA/local.

Incidente reversible del primer despliegue QA: CLI `vercel deploy --target preview` eliminó internamente ese target y Vercel asignó Production al primer intento del proyecto nuevo (`dpl_FyTiS81Eh4vwav21TRAUqQp7PW8m`). Tomó cero variables y terminó **ERROR**, nunca READY, antes de que la cancelación alcanzara el build. No se copiaron variables a Production ni se tocó el sitio productivo original. La fuente oficial instalada de CLI confirma esa regla de primer deployment. API rechazó `target:preview` con 400; su contrato define Preview omitiendo target. Conservando el primer intento fallido, la solicitud siguiente creó correctamente **`dpl_8DQuAmxwcWsQuhwuCosbNRJ2RcEd`**, target **null/Preview**, en el proyecto QA. La consulta posterior confirmó **READY**. URL única **https://universo-psi-mp-test-djjxzok6a-nmarcosan-2648s-projects.vercel.app**; alias asignado **https://universo-psi-mp-test.vercel.app**. Home y planes respondieron **200** sin cookie, bypass ni redirección. POST público a **https://universo-psi-mp-test.vercel.app/api/webhooks/mercado-pago/personal** devolvió **503 JSON `not_configured`**, respuesta del handler de la app esperada sin credenciales MP. No es evidencia de un webhook auténtico ni de pago. QA revisa además navegador e indexación.

### Configuración autorizada del Preview público de QA

Destino exclusivo: proyecto `universo-psi-mp-test` (`prj_nZDkEEpIowd5rSJbYBWfyhQuVZDl`), equipo `team_zbDtU9hfwTxtJvcj9sJMOp9u`, ambiente **Preview**. Base exclusiva: `gzsbndvaxdyuyhjidfqq`. Hobby activo, sin upgrade ni extras pagos; si aparece un límite o requisito de pago, detenerse. Se cargaron estos once nombres, sin valores en documentos o argumentos:

- `NEXT_PUBLIC_SUPABASE_URL`
- `NEXT_PUBLIC_SUPABASE_PUBLISHABLE_KEY`
- `SUPABASE_SECRET_KEY`
- `RATE_LIMIT_SALT`
- `CRON_SECRET`
- `NEXT_PUBLIC_SITE_URL`
- `MERCADOPAGO_ACTIVE_ACCOUNT`
- `MERCADOPAGO_PERSONAL_ENVIRONMENT`
- `MERCADOPAGO_CHECKOUT_ENABLED`
- `UNIVERSO_PSI_TEST_MODE`
- `SITE_NOINDEX`

Los tres secretos de servidor (`SUPABASE_SECRET_KEY`, `RATE_LIMIT_SALT`, `CRON_SECRET`) se cargaron como variables sensibles de Preview mediante stdin, sin logs ni argumentos con valores. Metadata remota confirmó las once variables **sólo Preview**, sin variables Production en el proyecto QA. Una lectura privada de Preview confirmó ambas flags exactamente `false`, la URL `gzsbndvaxdyuyhjidfqq` y el alias de pruebas. Cuenta personal y ambiente sandbox; no se incluyen token, clave pública ni secreto de webhook de Mercado Pago.

La publicación correcta usó `POST /v13/deployments` al ID exacto del proyecto QA con target omitido (Preview según API), archivos permitidos y settings Node24/Next.js. **No repetir a ciegas `vercel deploy --target preview` como primer deploy de un proyecto vacío:** la CLI normaliza el target y Vercel puede asignarlo Production, como demuestra el intento fallido documentado. El deployment final fue verificado target null/Preview. Snapshot de 121 fuentes de app/config/package: SHA256 `319f1e94dfa34b5e53ade77a4ffd6ff0d0f5d4b17fcb08456d527e7712b70426`, base commit `d9347fa02714890f1258fe540635f63d5787a9d0` más correcciones locales sin commit definitivo.

Revalidación posterior: QA detectó que `/planes` no mostraba el plan publicado y que el alias permitía indexación. El coordinador corrigió el mapeo de plan y agregó `X-Robots-Tag`/robots con `SITE_NOINDEX=true` o entorno Preview. Se agregó `SITE_NOINDEX=true` únicamente a Preview del proyecto QA. Correcciones verificadas con lint, TypeScript, 144 unitarias y build Node24; nuevo deployment `dpl_3qZo472htBz1F5LU8T5xEmoqTM4d`, target null/Preview, terminó ERROR al prerenderizar `/sitemap.xml` por lectura fallida de taxonomías públicas. No se movió el alias: conserva el Preview anterior READY. El coordinador corrigió sitemap para devolver vacío antes de consultar DB cuando SITE_NOINDEX=true o VERCEL_ENV=preview. Lint/typecheck/144 pruebas y build Node24 aprobaron. Datos verificó posteriormente 10/10 consultas exactas del DAL con HTTP200; el fallo remoto anterior no se reprodujo y no se atribuye con certeza a una causa transitoria. Nuevo intento dirigido **`dpl_DMwqRRXnxuEuAueboZjuVYX7vEXM` READY**, target **null/Preview**; alias **https://universo-psi-mp-test.vercel.app** actualizado a esta versión. URL única **https://universo-psi-mp-test-3hccnv77q-nmarcosan-2648s-projects.vercel.app**. QA recibió la versión exacta para repetir planes desktop/móvil, noindex/robots, sitemap vacío y webhook. Los resultados funcionales finales se registran en el informe QA. Snapshot actualizado de 122 fuentes SHA256 `35ab6d4806555b7b56cd032910bc8ddca9a3b854fc19a44f0f7f758a38de006d`, incluyendo la corrección de sitemap.

Auth sandbox se verificó con Site URL `https://universo-psi-mp-test.vercel.app`; allowlist de callbacks exactos para ese alias, el Preview protegido anterior y `http://localhost:3179/auth/callback`. No se cambió Auth productivo. El enlace temporal del Preview protegido se revocó después de validar el endpoint público nuevo.

Recuperación de esta preparación: detener nuevos deployments y conservar checkout cerrado; retirar únicamente el deployment/alias y variables del proyecto nuevo, o eliminar ese proyecto de QA tras preservar evidencias y coordinar la limpieza. Quitar el redirect nuevo de Auth sandbox si se agregó; conservar la base y su historial para análisis salvo autorización de borrado. El proyecto original, su SSO, el vínculo `.vercel` de raíz y Production permanecen intactos. Esta recuperación no cancela suscripciones de Mercado Pago: si futuras pruebas crean recursos del proveedor, su cierre debe comprobarse por separado.

## Vendedor sandbox validado el 12/09/2026

Después de que el usuario informó «Ahí cargué todo», se revisó el archivo local ignorado con permisos 0600: token, public key y secreto de webhook presentes, sin placeholders ni espacios internos; secreto con longitud compatible. Cuenta `personal`, ambiente `sandbox`, checkout y test mode `false`. El collector vacío se completó únicamente a partir de la respuesta del proveedor, sin inventarlo.

A las **13:38:27 UTC**, **GET `https://api.mercadopago.com/users/me` respondió 200**, con etiqueta **`test_user`**, sitio **MLA** e ID numérico válido. Se validó así el vendedor argentino de prueba mediante respuesta autoritativa, sin inferir ambiente por prefijo del token. El collector derivado se conservó en el archivo privado local y en evidencia mínima 0600, sin token, nombres o correo en este informe. La comparación en memoria confirmó que el correo comprador ingresado difiere del vendedor; **eso no valida que el comprador sea de prueba**.

En la validación inicial no se subieron credenciales ni se abrió checkout; la transferencia posterior autorizada se detalla a continuación. Este agente no hizo ningún POST a Mercado Pago. Public key y secreto sólo tienen presencia/formato comprobados: su pertenencia a la aplicación y una firma auténtica siguen pendientes de prueba integrada. Credenciales/identidad productivas no fueron validadas. Consultar las [credenciales oficiales de Mercado Pago](https://www.mercadopago.com.ar/developers/en/docs/credentials) para distinguir cuenta, aplicación y acceso.

### Credenciales Mercado Pago autorizadas exclusivamente en Preview

La revisión automática rechazó la transferencia posterior de credenciales MP y apertura de checkout: «aunque el sandbox está autorizado en general, no hay aprobación explícita de ese payload sensible para ese destino concreto». En ese momento no se ejecutó la acción ni se reintentó por otro medio. Después el usuario respondió explícitamente **«Sí, autorizo»** a esa transferencia concreta y checkout de prueba en ese destino. Con la nueva autorización se repitió GET `/users/me`: mismo collector, test_user y MLA; luego se cargaron cuatro variables MP sólo Preview y se estableció `MERCADOPAGO_CHECKOUT_ENABLED=true` únicamente allí. Metadata confirmó quince variables, ninguna Production; token y webhook secret sensibles. Una lectura privada verificó base sandbox, ambiente sandbox, TEST_MODE=false y SITE_NOINDEX=true. El nuevo deployment **`dpl_D8n8c3GicHUAFU3upir3ncffgHbc`** terminó **READY**, target **null/Preview**, y el alias **https://universo-psi-mp-test.vercel.app** ya apunta a él. URL única **https://universo-psi-mp-test-ctc1yu1p4-nmarcosan-2648s-projects.vercel.app**; misma fuente verificada SHA256 `35ab6d4806555b7b56cd032910bc8ddca9a3b854fc19a44f0f7f758a38de006d`.

Propuesta lista: cargar **únicamente** `MERCADOPAGO_PERSONAL_ACCESS_TOKEN`, `MERCADOPAGO_PERSONAL_PUBLIC_KEY`, `MERCADOPAGO_PERSONAL_WEBHOOK_SECRET` y `MERCADOPAGO_PERSONAL_COLLECTOR_ID` del vendedor `test_user`/MLA validado al ambiente **Preview** del proyecto Vercel **`universo-psi-mp-test`**, ID **`prj_nZDkEEpIowd5rSJbYBWfyhQuVZDl`**. Cambiar `MERCADOPAGO_CHECKOUT_ENABLED=true` sólo allí y redeploy Preview sobre el alias **https://universo-psi-mp-test.vercel.app**. Conservar Supabase **`gzsbndvaxdyuyhjidfqq`**, cuenta personal, ambiente sandbox, `UNIVERSO_PSI_TEST_MODE=false` y `SITE_NOINDEX=true`. Alcance comercial de prueba: mensual `PROFESSIONAL_MONTHLY`, **ARS120.000**, sin cambios de precio ni recursos productivos.

Archivo privado de la configuración autorizada: `/tmp/psi-remote-config/sandbox.mp-preview.proposed.env`, permisos 0600, quince nombres totales y sin email comprador. La transferencia ya se ejecutó después de la nueva autorización y validación fresca, sin imprimir PII ni credenciales. Token y webhook secret quedaron como sensibles sólo Preview; no hay credenciales MP en Production del proyecto QA. Se verificaron target Preview/READY y webhook público con body/query válidos sin firma: **401 `invalid_signature`**. Un probe **sintético firmado localmente**, tipo desconocido `deployment_check`, devolvió **200 `ignored=true`**; ese camino no consulta Mercado Pago ni escribe DB. Confirma coincidencia de secreto/configuración desplegada, **no** entrega auténtica del proveedor ni un pago. QA recibió el ID exacto para continuar su sesión y coordinar un único checkout.

Recuperación concreta: `MERCADOPAGO_CHECKOUT_ENABLED=false` sólo Preview y nuevo deploy compatible, conservar reconciliación/webhook y credenciales históricas si ya existen recursos de prueba. Cancelar autorizaciones de prueba creadas durante el recorrido únicamente con coordinación y verificar estado por API; el flag por sí solo no cancela débitos recurrentes. Nunca cambiar Production de Universo Psi. La coincidencia entre aplicación del token y aplicación del webhook todavía requiere confirmación; el GET de identidad del vendedor no demuestra ese vínculo. La API oficial de [gestión de aplicaciones](https://developers.mercadolibre.com.ar/es_ar/gestiona-tus-aplicaciones) requiere app_id conocido para detalles, y listar aplicaciones autorizadas no identificaría por sí solo cuál emitió el token. No se extrajo app_id del prefijo como prueba.

### Primer intento de checkout sandbox y diagnóstico de sólo lectura

QA hizo un único checkout por UI alrededor de las **13:59 UTC** sobre `dpl_D8n8c3GicHUAFU3upir3ncffgHbc`. Retornó a `/dashboard?subscription=checkout-error` sin abrir login de Mercado Pago. No se repitió el POST. Datos verificó después `provider_account=null`, sin vínculo del proveedor, `checkout_reserved=false`, cero recibos y cero eventos.

Release consultó **GET `/preapproval/search`** con la referencia externa exacta de esa suscripción: **HTTP200, total0, results0, coincidencias exactas0**. No se encontró un recurso del proveedor ni snapshot/application_id para inspeccionar. Esta búsqueda es evidencia de ese momento, no habilita automáticamente repetir un cobro. Identificadores y evidencia mínima se conservaron sólo en `/tmp/psi-remote-config/checkout-attempt-mp-search.json` con permisos0600.

Logs de Vercel acotados al deployment y ventana **13:58:30–14:01 UTC** mostraron solamente POST `/dashboard`200 y GET `/dashboard`200; ambos sin mensajes ni logs internos. No hay código de error del proveedor registrado en esos resultados. El estado sin reserva sugiere fallo previo a reservar, pero la causa debe confirmarse en código/contrato antes de repetir. Logs privados sin publicar PII; no se hicieron mutaciones de Mercado Pago, Vercel ni DB durante este diagnóstico.

Diagnóstico adicional de configuración y lookup: las **diez variables exportables** del Preview coinciden con el archivo autorizado, sin diferencias (incluidos collector, public key, flags y URL de base). Las otras cinco son sensibles/no exportables y Vercel devuelve `[SENSITIVE]`; por eso no se afirma igualdad obtenida por reexportación. Se conservó la evidencia de carga por stdin y la prueba sintética de firma del secreto desplegado. GET `/users/me` con la misma credencial local cargada respondió nuevamente **200**, ID numérico, test_user, MLA y collector coincidente. La RPC de sólo consulta `lookup_plan_provider_id(PROFESSIONAL_MONTHLY, personal)` con service key del sandbox respondió **HTTP200, null**, resultado válido para una suscripción sin plan externo preasociado. No se llamó `begin_subscription_checkout`, ni se reservó ni creó un recurso MP. La revisión posterior del coordinador identificó la causa en la acción de suscripción: filtraba directamente por la columna protegida `user_id`, lo que devolvía403 antes de reservar. Se corrigió usando la RPC propia `my_professional_profile`, sin ampliar grants ni cambiar DB. Datos verificó JWT propio200/1perfil, anon401 y filtro ajeno0. Diagnósticos de checkout añadidos con etapas fijas y sin PII; lint/typecheck/150 pruebas y build Node24 aprobados. Nuevo Preview **`dpl_Fqqjowr2VNcpHxQjjzu3YULbNYgd` READY**, target **null/Preview**, con las mismas quince variables autorizadas. Alias **https://universo-psi-mp-test.vercel.app** actualizado; URL única **https://universo-psi-mp-test-6fnopu6uu-nmarcosan-2648s-projects.vercel.app**. QA recibió la versión exacta para refrescar y repetir un único intento coordinado sobre el mismo fixture. Fuente de 124 archivos SHA256 `922ef5cb49f9181a3b5538f0d6923985d13c8e24486f384d8e1d94abdc1df520`.

### Intento único posterior al fix: rechazo del proveedor

A las **14:14:19 UTC**, QA hizo el único intento coordinado posterior al fix, sobre `dpl_Fqqjowr2VNcpHxQjjzu3YULbNYgd`. Retornó checkout-error. Esta vez datos confirmó **reserva=true y cuenta personal**, sin vínculo, recibos, eventos ni período pagado. Es un fallo distinto del filtro de perfil anterior, ya corregido.

Los logs acotados **14:14–14:16 UTC** registraron `mercado_pago_checkout_failed`, **stage=`provider_create`**, reason=`operation_failed`, **providerStatus=400**. El POST de la Server Action respondió200 por la redirección de la aplicación; no significa éxito de Mercado Pago. No se registró el cuerpo del400, por lo que su causa concreta no puede deducirse del mensaje.

GET `/preapproval/search` con external_reference exacta respondió **200, total0/results0/coincidencias0**. No se encontró un recurso y no se reintentó creación ni se liberó la reserva. Evidencia mínima privada0600 en `/tmp/psi-remote-config/checkout-retry-vercel-logs.jsonl` y `checkout-retry-mp-search.json`. Mantener reserva hasta resolver el resultado/cuenta y la causa del400; esta consulta no es autorización para volver a cobrar.

### Identidad del comprador indicada por el usuario

La captura oficial posterior de Cuentas de prueba identifica un vendedor y un comprador argentinos de la aplicación indicada. El collector configurado coincide con el vendedor de la captura. GET público `/users/{buyer_id}` a las **14:38 UTC** respondió **200**, con ID comprador coincidente, sitio MLA y distinto del vendedor. Sin embargo, esa respuesta **omite por completo `tags` y `email`**. No permite validar la etiqueta test_user ni obtener el correo del comprador; la ausencia del campo no demuestra que sea una cuenta real.

No se dedujo correo desde ID/nickname ni se modificó `MERCADOPAGO_SANDBOX_BUYER_EMAIL`. Se requiere obtener el correo desde la sesión del comprador o una lectura autorizada de `/users/me` de esa cuenta. QA fue informado y preserva el fixture previo con su reserva. Las contraseñas/códigos test aportados no se reprodujeron ni se usaron para estas lecturas. La aplicación identificada en la captura sigue separada de la prueba pendiente de `application_id` de un recurso emitido por el token.

Actualización posterior del comprador: QA autenticó la cuenta de prueba indicada en una sesión separada y obtuvo el correo completo, sin máscara, desde `/accounts/profile-data`, con username exacto coincidente. Evidencia privada0600: `/tmp/psi-mp-buyer-email-authoritative-20260912.json`. QA actualizó únicamente `MERCADOPAGO_SANDBOX_BUYER_EMAIL` local; no se publican correo, contraseña ni códigos. Esta evidencia de sesión complementa el GET público de ID/MLA; no convierte el correo anterior ni su reserva en el nuevo fixture.

Helper preparado, todavía sin ejecutar contra el nuevo fixture: `/tmp/psi-remote-config/verify_new_sandbox_preapproval.py`, permisos0600 y sintaxis compilada. Recibe `--identity-file` nuevo y `--evidence-file` bajo el directorio privado; rechaza la suscripción antigua, compara email en memoria con la evidencia autoritativa y revalida vendedor test_user/MLA. Después sólo ejecuta GET search por referencia exacta y GET del recurso encontrado: compara collector, application_id con captura, payer/email si expuestos, moneda/importe/frecuencia y sandbox explícito si existe `live_mode`. La ausencia de campos permanece como desconocida; no se inventa modo ni identidad. IDs y resultados mínimos permanecen en evidencia privada, sin PII en stdout. No incluye POST, reset, reserva, cambio de variables ni escritura DB.

La configuración de webhook mantiene evidencia existente: URL pública personal del Preview, secreto desplegado que pasó probe sintético firmado y prueba sinfirma401. No hay todavía evidencia de evento auténtico ni prueba de que la aplicación que firmará eventos sea la misma que emitió el token. La API oficial de gestión permite consultar una app por ID conocido o listar grants, pero no se encontró un método documentado de introspección que identifique unívocamente la aplicación emisora de este token sin contexto adicional. Se verificará `application_id` del recurso cuando exista; no se usarán prefijos como prueba.

Nuevo fixture comprador: Auth/login completados por QA, pero el único submit de aceptación de términos alrededor de **15:00 UTC** mostró error antes de onboarding/checkout. Logs read-only del Preview vigente confirmaron POST `/aceptar-terminos` **15:00:38UTC200** y GET posterior **15:00:44UTC200**, sin logs internos, código o etapa diagnóstica. No se repitió submit desde release ni se hicieron consultas MP del fixture nuevo. El HTTP200 de la acción no demuestra aceptación; causa pendiente de diagnóstico de aplicación/datos. Evidencia privada0600 en `/tmp/psi-remote-config/buyer-terms-error-vercel-logs.jsonl`.

### Nuevo comprador: checkout pendiente vinculado y aplicación identificada

El único checkout del nuevo fixture con correo autoritativo del comprador, a las **15:08:50 UTC**, llegó al redirect de Suscripciones de Mercado Pago. QA no ingresó tarjeta ni confirmó pago. Datos verificó vínculo del proveedor y cuenta personal en **PENDING**, sin período pagado, eventos ni recibos.

A las **15:10:23 UTC**, release revalidó vendedor test y consultó GETsearch exacto: **200, una coincidencia**; GETpreapproval **200, pending**. Coinciden ID del recurso con vínculo DB, external_reference, collector, payer_id del comprador indicado y snapshot **ARS120.000, frecuencia1 months**. El recurso no expone payer_email ni live_mode: permanecen desconocidos; la prueba de cuenta sandbox proviene del vendedor test_user/MLA y de la identidad de comprador corroborada, no de campos ausentes.

**Diferencia de aplicación confirmada por el recurso:** `application_id=2796628634374866`, mientras la captura inicial correspondía a **2533766701506518**. No basta compartir vendedor o cuentas de prueba: verificar/configurar webhook y secreto en la aplicación que efectivamente creó el recurso. El probe sintético200 demuestra igualdad del secreto cargado, pero no demuestra que esa app vaya a firmar eventos con él. Root y QA recibieron el hallazgo antes de continuar el pago. Evidencia mínima privada0600: `/tmp/psi-remote-config/verified-buyer-preapproval-20260912.json`; no emails, tokens, tarjeta ni passwords en este informe.

No se creó un segundo recurso ni se liberó la reserva anterior. No se realizó cargo ni se modificó Mercado Pago/Vercel/DB durante la verificación. La suscripción pending y el redirect demuestran creación/vínculo, **no pago aprobado ni beneficios activos**.

### Confirmación de prueba y lectura autoritativa posterior

QA confirmó una sola vez el checkout sandbox a las **15:27:09 UTC**, con cuentas de prueba corroboradas y tarjeta oficial de prueba; Mercado Pago mostró la suscripción confirmada. GET posterior a las **15:28 UTC** verificó preapproval **authorized**, próximo pago **12/10/2026**, y búsqueda de pagos con una coincidencia. GETpayment devolvió **approved/accredited, ARS120.000**, con external_reference, collector e importe coincidentes. IDs permanecen en `/tmp/psi-remote-config/buyer-payment-after-confirmation-20260912.json`0600.

El pago reporta **`live_mode=true`**, `operation_type=regular_payment`, tags null y metadata vacía. No se infiere de ese flag aislado que haya sido un cargo real: la prueba usó vendedor test_user/MLA revalidado, comprador de prueba autenticado y tarjeta de prueba. Tampoco se oculta la discrepancia: el modo del recurso requiere tratamiento explícito y la aplicación no debe relajar sus guardias por mera suposición. No hubo reembolsos, creación adicional ni otras escrituras del proveedor durante el diagnóstico.

Logs de la ventana **15:26–15:30 UTC** mostraron **cero requests al webhook** del Preview. El observador de datos continuaba en PENDING_PAYMENT, sin eventos, recibos ni período pagado. La ausencia de entrega y la configuración de webhook de la app emisora siguen pendientes; no se atribuye el pago aprobado a persistencia/beneficios aún.

La búsqueda de facturas con `limit=50` respondió **400 `Invalid value for limit`**. La [referencia oficial](https://www.mercadopago.com.ar/developers/es/reference/online-payments/subscriptions/authorized-payment-search/get) muestra preapproval_id y un ejemplo con paging.limit20; sin embargo, la comprobaciónGET con20 también devolvió400, por lo que ese cambio no está validado. De esa segunda respuesta no se conservó el cuerpo: no se afirma que fuera el mismo mensaje. La última consulta sólo con preapproval_id, omitiendo limit/offset, devolvió **429 `local_rate_limited`**; se detuvieron las variantes. No se registró el header Retry-After y no se volvió a consultar sólo para recuperarlo. No tratar el fallo de consulta como ausencia de factura ni aplicar un límite20 como solución probada. La reconciliación necesita resolver el contrato de búsqueda y verificar la factura/pago sin duplicar cobros.

### Factura recuperada tras cooldown y acceso a la aplicación

A las **19:11:03 UTC**, después de más de tres horas desde el429, se realizó **una sola consulta** GET de facturas con `preapproval_id` exacto, sin limit/offset: **HTTP200**, paging **offset0/limit12/total1**. Devolvió una factura **processed**, importe **ARS120.000**, suscripción coincidente y pago **approved**. Headers Retry-After y X-RateLimit-Remaining ausentes. No se ejecutaron más consultas en esta comprobación. Evidencia mínima privada0600: `/tmp/psi-remote-config/buyer-invoices-after-cooldown.json`. El límite predeterminado observado es12; eso no prueba un máximo universal ni valida cambiar a20.

La captura de la aplicación con error genérico no permite afirmar una caída del proveedor. Contexto autoritativo disponible: token→vendedor test_user/MLA; recurso→mismo collector y aplicación **2796628634374866**. No confirma la cuenta de la sesión de navegador donde apareció el error. La [guía oficial de cuentas de prueba](https://www.mercadopago.com.ar/developers/es/docs/your-integrations/test/accounts) asigna al vendedor la configuración de aplicación y credenciales. Revisar en una sesión separada del **vendedor TEST**: Developers → Tus integraciones → app2796628634374866 → Webhooks, si ese panel está disponible. La app inicial2533766701506518 y la emisora no tienen por qué ser idénticas; la configuración debe corresponder a la emisora del recurso. No se accedió a una sesión personal ni se hicieron escrituras.

El GET individual posterior de esa factura respondió **200**, status **processed**, type **recurring**; coinciden ID, preapproval, external_reference e ID del pago aprobado ya consultado, con **ARS120.000** y nested payment **approved/accredited**. La factura no expone collector_id ni live_mode; el collector se correlaciona con preapproval/pago autoritativos previos. Evidencia mínima privada0600 en `/tmp/psi-remote-config/buyer-invoice-authoritative-detail.json`, sin PII. El helper de observación quedó preparado para omitir limit/offset en búsqueda de facturas y declarar si la página cubre el total; no se volvió a ejecutar. La aplicación sigue requiriendo su corrección de paginación y tratamiento de modo, además de webhook/reconciliación integrada.

### Validación real del adaptador recurrente corregido

La nueva `fetchRecurringPayment(invoiceId, personal)` pasó **una ejecución real de sólo lectura**, usando Node24/Vitest privado y el módulo actual de la aplicación. Se realizaron exactamente **cuatro GET HTTP200**: vendedor, factura, preapproval y pago. Guardia vendedor test_user/MLA y collector correctos; IDs, referencias, importes y monedas coinciden; pago **approved, ARS120.000**. No se importó reconciliación/DB, no hubo RPC ni POST, y el proveedor no fue simulado (sólo stub de `server-only` para ejecutar el módulo).

La corrección permite tratar el `live_mode=true` observado en una cadena recurrente de cuentaTEST únicamente después de validar identidad y todos los recursos autenticados; no es una excepción general para pagos sueltos. La guardia de producción continúa exigiendo identidad noTEST/modo correspondiente. Evidencia segura0600 en `/tmp/psi-remote-config/recurring-readonly-smoke/result.json`; logs privados. El despliegue sigue pendiente de checks integrados/build del coordinador; esta prueba no demuestra persistencia ni webhook auténtico.

Publicación de las correcciones recurrentes: el coordinador completó lint, TypeScript, **190 pruebas en11 archivos** y build DockerNode24. Con el smoke real4GET aprobado se tomó snapshot estable **SHA256 c7e6628de36fa3f22bbe9d4c274bf0495ad465f59944759ad6339a674411d6a3**,124fuentes/180archivos de upload excluyendo secretos. Nuevo deployment **dpl_E1cjBmAaM2Sk91sQEptSXuqbMVP6 READY**, target **null/Preview**; mismas quince variables autorizadas, sin cambios de ambiente. Alias **https://universo-psi-mp-test.vercel.app** actualizado; URL única **https://universo-psi-mp-test-li0vhwwv7-nmarcosan-2648s-projects.vercel.app**. Smoke real GETwebhook405 y POSTsinfirmа401, sin redirección, con X-Robots-Tag noindex/nofollow/noarchive. No se atribuyen esas solicitudes sintéticas al proveedor. El dashboard no ofrece una acción de reconciliación para este usuario; root coordina prueba administrativa separada sobre el único recurso existente, no otro checkout.

Prueba administrativa preparada y primera ejecución autorizada completada: helper privado `/tmp/psi-remote-config/recurring-recovery-smoke/` importa la función real `reconcileSubscriptionResources`, con proveedor y DB reales. Guardia de red limita MP a GET de recursos exactos del fixture y Supabase al origen sandbox; sólo permite POST a las RPC `apply_subscription_webhook_event` y `apply_subscription_payment_event`, una vez cada una por ejecución, con suscripción/preapproval/pago conocidos, cuenta personal y ARS120.000. No permite crear pagos, reservar, migrar ni tocar Production. Root registró baseline previo con un vínculoPENDING y cero eventos/recibos/período, revisó las guardias y autorizó la primera recuperación. La ejecución terminó **exit0/reconciled1**, con **8GET MP200 y exactamente2RPC sandbox200**; todas sin Retry-After, sin429 ni timeout y sin reintentos. Evidencia privada0600 en `/tmp/psi-remote-config/recurring-recovery-smoke/result-first.json`. El observador posterior de root confirmó **ACTIVE, un recibo, dos eventos, período pagado activo y last_payment_at presente**. El perfil permanece **DRAFT/NOT_VERIFIED**, con señal de ranking nula: pagar no omite la revisión profesional. Evidencia `/tmp/psi-hosted-payment-after_recovery_buyer-20260912.json`. Después de esa observación, root autorizó una única recuperación duplicada sobre el mismo recurso: también exit0/reconciled1,8GET200+2RPC200, sin429/timeouts; `/tmp/psi-remote-config/recurring-recovery-smoke/result-duplicate.json`0600. Root completó el diff posterior: **igualdad exacta** de subscription, payment_state, recibos, eventos, conteos, perfil y ranking. Se conservan **ACTIVE, un recibo, dos eventos y el mismo período**, sin duplicar ni extender beneficios. El navegador real recargó con HTTP200, mostró suscripción activa y cero botones de checkout. Persistencia e idempotencia verificadas mediante recuperación administrativa; entrega auténtica de webhook y acceso/configuración del panel de la app emisora siguen pendientes. Recuperación administrativa no equivale a entrega auténtica de webhook.

### Configuración de webhook de la aplicación emisora confirmada por el usuario

El usuario accedió al panel correcto del vendedor TEST y aplicación **2796628634374866**, y guardó Webhooks en **Modo productivo de esa cuenta de prueba**, URL **https://universo-psi-mp-test.vercel.app/api/webhooks/mercado-pago/personal**, tópicos **Planes y suscripciones** y **Pagos legacy**. Esto no habilita Production de Universo Psi. Después guardó el secreto nuevo en el archivo local privado0600; comparación en memoria confirmó secreto cambiado y resto de credenciales/cuenta/ambiente idénticos.

Se actualizó **sólo `MERCADOPAGO_PERSONAL_WEBHOOK_SECRET` sensible en Preview QA**; las otras catorce variables permanecen iguales. Snapshot de código confirmado idéntico **c7e6628de36fa3f22bbe9d4c274bf0495ad465f59944759ad6339a674411d6a3**. Nuevo deployment **dpl_8JpgqxExebgPN5L94BET9uw7ahRH READY**, target **null/Preview**. Alias **https://universo-psi-mp-test.vercel.app** actualizado; URL única **https://universo-psi-mp-test-misfs2047-nmarcosan-2648s-projects.vercel.app**. Se comprobaron **unsigned401 invalid_signature** y **probe desconocido firmado200 ignored=true**, sin redirección, como pruebas de configuración, no como webhook auténtico. Evidencia privada0600 en `/tmp/psi-remote-config/new-webhook-secret-config-smoke.json`. No hubo llamadasMP, escriturasDB ni pagos durante este cambio. El bloqueo de acceso al panel quedó resuelto; resta observar entrega real firmada y persistencia del evento.

### Segunda rotación del secreto tras regeneración del usuario

El usuario regeneró el secreto después de una captura donde había quedado visible y lo guardó nuevamente en el archivo local0600. La comparación privada confirmó valor cambiado y resto de credenciales idénticos. Antes de desplegar se detectó SHA distinto: la revisión demostró **sólo dos líneas de comentario** en `reconcile-search.ts`; restaurarlas en memoria reproduce exactamente el SHA anteriorc7e6628..., y todos los demás bytes coinciden. Comentario revisado y autorizado; SHA real nuevo **7d2d86c58ef28350f954e233c4585fb273931c68d57cad65122427f4e4da2d61**, sin cambios funcionales ni repetición innecesaria de tests completos.

Se rotó únicamente `MERCADOPAGO_PERSONAL_WEBHOOK_SECRET` sensible en PreviewQA. Nuevo deployment **dpl_GZ5isPbTsuXdKvyKNqWsgb1pwMQr READY**, target **null/Preview**. Alias **https://universo-psi-mp-test.vercel.app** actualizado; URL única **https://universo-psi-mp-test-qxcgcf9x5-nmarcosan-2648s-projects.vercel.app**. Probes con tipo desconocido sintético confirmaron **firma nueva200 ignored=true**, **firma anterior401 invalid_signature** y **sin firma401**, sin redirección, llamadasMP ni escriturasDB. El archivo temporal0600 del secreto anterior se eliminó tras el probe negativo aprobado; otras copias privadas permanecieron intactas. Evidencia mínima sin secretos en `/tmp/psi-remote-config/webhook-secret-rotation-smoke.json`0600. Esta rotación y sus probes no prueban notificación auténtica del proveedor.

### Simulador de Mercado Pago correlacionado con Vercel

El usuario envió desde el panel de la app emisora una simulación `subscription_preapproval`, usando el ID del preapproval real, y reportó **200OK**. Vercel registra el POST correspondiente a las **19:54:42.345 UTC** del12/09/2026 en **`/api/webhooks/mercado-pago/personal`**, dominio **universo-psi-mp-test.vercel.app**, deployment **dpl_GZ5isPbTsuXdKvyKNqWsgb1pwMQr**, **HTTP200**, sin errores internos. Es posterior y distinguible de los probes locales de rotación19:53:59–19:54:00, que devolvieron401/401/200. Correlación privada0600: `/tmp/psi-remote-config/mp-simulator-webhook-correlation.json`.

Esto verifica **conectividad y validación de firma de una simulación emitida por Mercado Pago**; no es un evento comercial automático nuevo ni evidencia de otro pago. La firma protege el manifiesto que incluye data.id, x-request-id y ts; no convierte type/action/application_id/id/fecha del payload en metadatos firmados. El handler obtiene el estado autoritativo del recurso mediante API. El observador de root comprobará el efecto persistido/idempotencia sin atribuir cambios a metadatos simulados. No se hicieron escrituras del proveedor, DB, variables o deploy desde este diagnóstico de logs.

### Observación acotada de la actualización automática y brecha del tópico payment

El coordinador realizó una única actualización autorizada de **reason** sobre el preapproval TEST ya verificado: respuesta API **200 a las 20:00:14.758 UTC** del 12/09/2026, last_modified **20:00:14.639 UTC**. Comparó trece campos comerciales y todos permanecieron iguales; no creó pagos ni ejecutó reconciliación o escrituras DB. Evidencia privada0600: `/tmp/psi-remote-config/automatic-webhook-reason-result.json`.

Release consultó logs del deployment vigente **dpl_GZ5isPbTsuXdKvyKNqWsgb1pwMQr**, filtrando desde **20:00:14 UTC** y excluyendo la simulación19:54. Las lecturas observadas a **20:00:53.354 UTC** y **20:02:19.512 UTC**, con una espera intermedia, registraron **cero POST** al webhook personal. Evidencias privadas0600: `automatic-webhook-correlation-first.json` y `automatic-webhook-correlation-second.json` en `/tmp/psi-remote-config/`. Root observó que DB conserva **ACTIVE, un recibo y dos eventos**. Esto delimita una ventana sin entrega observable; no identifica la causa ni demuestra que no pueda llegar posteriormente. El usuario indicó que el panel de monitoreo TEST no abre; no se le pide repetir ese acceso.

La revisión detectó además una brecha concreta independiente de esa ausencia de entrega: el tópico **payment** puede corresponder a una cuota recurrente, mientras la ruta desplegada todavía lo trata como pago único. La corrección en preparación determina el modelo por la referencia autenticada y la suscripción persistida, valida la factura recurrente y utiliza la RPC/evento recurrente con su misma clave de deduplicación. Está pendiente de checks integrados, prueba real acotada y despliegue QA; **el recorrido automático de extremo a extremo sigue incompleto**.

Se preparó, sin ejecutarlo, `/tmp/psi-remote-config/payment-notification-smoke/run.py`, que importa `reconcilePaymentNotification` real. Limita Mercado Pago a GET de vendedor y recursos conocidos, y Supabase al proyecto sandbox: una lectura exacta de la suscripción fixture y una sola RPC `apply_subscription_payment_event` del pago recurrente existente, por ARS120.000. Exige baseline ACTIVE, vendedor TEST fresco y vínculo exacto; no permite crear pagos, tocar otra suscripción, migrar ni acceder a Production. Las fases first/duplicate tienen marcador exclusivo previo a toda red y evidencias separadas; cada ejecución requiere baseline y autorización del coordinador. No se ejecutó API, recuperación, despliegue ni cambio de variables en esta preparación.

Primera ejecución del helper payment autorizada tras lint, TypeScript, **222 pruebas** y build Node24 aprobados: se detuvo en aproximadamente405ms con **TypeError**, antes de obtener ninguna respuesta HTTP (`requests=[]`) y sin RPC. Se preservaron marcador exclusivo, resultado y log privados; no hubo reintento automático. No se atribuye todavía la causa a red ni al proveedor: se preparó diagnóstico limitado a etapa y códigos de transporte permitidos para una eventual ejecución autorizada. El fallo de infraestructura/prueba aún requiere resolución antes de publicar esta corrección; no es evidencia de error comercial ni persistencia nueva.

El coordinador autorizó después una fase separada **first-network**, con acceso de red y las mismas guardias/baseline, preservando el intento inicial. Resultado **PASS/exit0**: siete GET Mercado Pago200, una lectura exacta de suscripción sandbox200 y **una sola RPC recurrente200**. Vendedor TEST fresco confirmado; sin429, timeout ni Retry-After. Evidencia0600: `/tmp/psi-remote-config/payment-notification-smoke/result-first-network.json`. No hubo creación de pagos ni cambios comerciales. El observador posterior de DB, la fase duplicada y el despliegue esperan coordinación; esta ejecución directa de la función no prueba recepción automática de webhook.

Después de first-network, el observador independiente de root confirmó **igualdad exacta de ocho grupos** respecto del baseline: ACTIVE, un recibo, dos eventos y período idéntico. El tópico payment ya actuó como duplicado entre tópicos sobre el pago reconciliado previamente; no se repitió una fase duplicate innecesaria. QA revisó seis archivos sin defectos en ese recorrido.

Con checks integrados aprobados se publicó exclusivamente QA **dpl_FyJf4DsMS3BUJZKxueNtQTCgbiBh READY, target null/Preview**, fuente **SHA2563621eedd44687ab8c633ca7d051c21b9526c1fe1ab3e22f5e82cce97caf81483**, mismas quince variables sin modificaciones. URL única **https://universo-psi-mp-test-h4m9lalqs-nmarcosan-2648s-projects.vercel.app** y alias QA **https://universo-psi-mp-test.vercel.app**. El alias terminó de actualizarse cuando llegó la pausa del coordinador por un hallazgo adicional: compatibilidad con el estado oficial `canceled` de preapproval. **No se ejecutó el smoke firmado del handler ni otro probe**. Esa normalización está pendiente de integración/checks y próximo deployment antes de continuar; no se revirtió por cuenta propia. Production y DB permanecen intactas.

### Preview integrado y smoke del tópico payment desplegado

La normalización acepta `canceled` y `cancelled` de preapproval y conserva el estado interno CANCELED. El coordinador aprobó lint, TypeScript, **228 pruebas en11 archivos** y build Node24; fuentes estables. Nuevo **dpl_3VZzHsZxEtbMSMCsdznzJu1tpgcv READY**, target **null/Preview**, SHA256 **4f33a26bc6a1c1df7bdc0bef9537241a2f8c09781044a5eca879c06aebd314a2**. Alias **https://universo-psi-mp-test.vercel.app** actualizado; URL única **https://universo-psi-mp-test-i139y2tst-nmarcosan-2648s-projects.vercel.app**. Mismas quince variables; no cambios de Production, credenciales o migraciones.

Smoke ejecutado exactamente una vez: **GET405 a las20:15:53.010 UTC**, **POST sinfirma401 invalid_signature a las20:15:54.058 UTC**, y **POST topic payment firmado localmente sobre el pago TEST existente200 ok=true a las20:15:54.611 UTC**. Sin redirecciones. Este último recorrió el handler desplegado y su consulta/persistencia reales; fue una notificación **sintética local**, no una entrega automática de Mercado Pago. Evidencia privada0600: `/tmp/psi-remote-config/payment-topic-deployed-smoke-result.json`, con marcador exclusivo que impide repetirlo. Root verificará DB/navegador posteriores; no se ejecutará cancelación ni otro evento desde release sin coordinación. La observación de entrega automática continúa pendiente.

El observador posterior al smoke desplegado confirmó igualdad exacta de ocho grupos con el baseline: **ACTIVE, un recibo, dos eventos y período idéntico**. El navegador QA respondió200, mostró suscripción activa y ningún botón de checkout. Evidencia `/tmp/psi-hosted-payment-after_payment_topic_deploy_buyer-20260912.json`; un primer intento de navegador abortó por estar en about:blank, antes de solicitudes al sitio.

### Cancelación TEST y primeras entregas automáticas observadas

QA/root ensayaron la baja del recurso TEST existente. El primer PUT con `canceled` recibió **400 a las20:18:42 UTC**; lectura posterior confirmó authorized y last_modified sin cambio. Fuentes primarias presentan ambas grafías: tras revisar el SDK oficial, un único intento separado con `cancelled` obtuvo **HTTP200 a las20:23:05 UTC**. El helper validó respuesta cancelada y vínculo, pero se detuvo ante cambio de otros campos comerciales; no hubo tercer PUT, reversión ni GET final desde ese helper. El coordinador verifica esos campos por lectura independiente. No se asume pago adicional ni causa exacta del primer rechazo.

Release observó desde **20:23:03 UTC**, sin simuladores ni recuperación en la ventana. La primera consulta realizada a **20:24:23 UTC** detectó POST automáticos al webhook personal a **20:23:10.727** y **20:23:11.970 UTC**, ambos **HTTP502**, deployment **dpl_3VZzHsZxEtbMSMCsdznzJu1tpgcv** y dominio QA. Los logs internos contienen únicamente la etapa fija **mercado_pago_reconciliation_failed**, cuenta personal y tópico **subscription_preapproval**. La ruta alcanza esa etapa después de validar firma: existe evidencia de **entrega automática con firma aceptada**, pero reconciliación fallida; no se presenta como persistencia exitosa. La segunda lectura a **20:25:22 UTC** mostró las mismas dos solicitudes, sin nuevas entregas. Evidencias privadas0600: `/tmp/psi-remote-config/cancel-webhook-correlation-30s.json`, `cancel-webhook-correlation-2m.json` y `cancel-webhook-log-diagnostics.json`. El coordinador detuvo el tercer control antes de ejecutarse al identificar la causa; no hubo escrituras desde release durante esa observación.

Diagnóstico reproducible del coordinador: el GET del preapproval cancelado **omite init_point**, pero el DTO de lectura exigía checkoutUrl, causando rechazo antes de persistir la baja. Se prepara admitir URL ausente para lectura/reconciliación, manteniendo URL obligatoria para crear o reanudar checkout. No requiere cambiar contrato RPC ni modificar otra vez Mercado Pago. Se detuvo la tercera consulta de logs prevista para cinco minutos; se conservaron las dos lecturas502. Próximos pasos: checks integrados, deploy QA corregido y ventana definida para observar reintento auténtico, sin recuperación DB ni simulación antes de esa observación.

El ajuste de lectura nullable quedó integrado, con guardia de URL obligatoria al iniciar/reanudar checkout. Root aprobó lint, TypeScript, **240 pruebas en11 archivos**, build Node24 y smoke real cancelado: dos GET200, statuscancelled/initPointnull, sin DB ni mocks. Se intentó publicar QA con las mismas quince variables y SHA256 **f8bc8c856e87e9e1f9d1f02bd84fd750cbb3384ada31d4a50bc90c17dfde6bf0**: deployment **dpl_B2Sgd12rg9YaN3oB7Cd6rJS72ac2** terminó **ERROR**, BUILD_UTILS_SPAWN_1. Log de build a las20:30:11–20:30:12 UTC: «No se pudieron cargar las taxonomías públicas» y «Failed to collect page data for /profesionales/[slug]». No se atribuye a fallo transitorio sin evidencia. **El alias permanece en dpl_3VZzHsZxEtbMSMCsdznzJu1tpgcv**; no se enviaron probes ni recuperación. El coordinador resolverá esa dependencia del build antes de autorizar otro intento; todo reintento auténtico deberá correlacionarse con el deployment que efectivamente lo recibió.

Revisión local adicional del export Production: `MERCADOPAGO_ACTIVE_ACCOUNT` y las tres credencialesPERSONAL están presentes pero exportadas como **[SENSITIVE]**; la cuenta activa real tampoco es verificable como enum a partir de ese archivo. El tokenCOMPANY está ausente. No se usaron placeholders como valores reales ni se llamó al proveedor. Sin un token real disponible por un medio autorizado, sigue pendiente validar identidad/cuenta receptora productiva; no se solicitan secretos por chat ni se cambian variables de producción para diagnosticar.

Lectura de logs por **proyecto QA** a las **20:38:24 UTC**, desde20:23:03 y aproximadamente quince minutos después de la cancelación: permanecen únicamente las dos solicitudes502 iniciales, ambas en **dpl_3VZzHsZxEtbMSMCsdznzJu1tpgcv**. No se observaron nuevos reintentos hasta esa lectura. Evidencia privada0600 `/tmp/psi-remote-config/cancel-retry-webhook-correlation-retry15m.json`. No se presume un horario garantizado del proveedor ni ausencia definitiva de reintentos. Se detuvieron consultas adicionales hasta nuevo READY o coordinación; no se intervino en DB ni se enviaron eventos sintéticos.

### Cancelación: versión corregida READY y reintento sobre la versión anterior

El guard de `generateStaticParams` evita consultar catálogo durante el build QA/noindex, conservando la conducta productiva y las consultas al visitar perfiles. Datos comprobó diez consultas exactas del DAL200 desde este equipo; eso no identifica la causa del build remoto previo. Root completó lint, TypeScript, **240 pruebas en11 archivos** y build DockerNode24 con SITE_NOINDEX=true. Snapshot **SHA256 a68593b8d4d5e959ef65df607007674b14cebbed42b3d7a0cbb4c3c7ae0f46c6**; deployment **dpl_42VMAv7sM5jC6wtHDggP6GDy5sPj READY**, target **null/Preview**, mismas quince variables sin cambios. Alias QA actualizado a las **20:41:37.632 UTC**; URL única **https://universo-psi-mp-test-jecj3h3ft-nmarcosan-2648s-projects.vercel.app**. Sin POST sintético, recuperación manual ni escriturasDB durante publicación/observación.

La lectura acotada de logs del proyecto a **20:42:07.817 UTC** detectó un nuevo reintento automático a **20:40:44.407 UTC**, HTTP502, recibido por **dpl_3VZzHsZxEtbMSMCsdznzJu1tpgcv**, antes del cambio de alias. Se conservan además las dos solicitudes502 iniciales. **Ninguna de esas tres solicitudes llegó al deployment corregido42VM**; no representan un fallo comprobado de la corrección. Evidencia0600: `/tmp/psi-remote-config/cancel-retry-webhook-correlation-ready.json`. Queda observar un próximo reintento en la versión actual y su estado persistido.

La [documentación oficial vigente de Webhooks](https://www.mercadopago.com.ar/developers/es/docs/checkout-bricks/additional-content/your-integrations/notifications/webhooks) describe el **Panel de notificaciones dentro de Webhooks de la aplicación**, con lista de entregas, filtros y detalle al abrir una fila; no se confirmó documentalmente un botón de reenvío manual. Si el usuario puede entrar en Webhooks de la app emisora, el paso concreto es abrir el detalle de la notificación fallida de las17:23ARG. No confundirlo con Simular ni pedir reiterar el Panel de monitoreo que no abre. Esa guía documenta reintentos inicialmente cada15min y demora ampliada después del tercero; no se presupone un horario exacto. La ruta genérica antigua /docs/your-integrations/notifications/webhooks respondió404 al revisar.

La única lectura programada para20:56UTC terminó a las **20:56:04.422 UTC**: conserva **tres POST502**, todos recibidos por el deployment anterior3VZz; el más reciente sigue siendo20:40:44.407. **No hay request nuevo ni200 observable en el deployment corregido42VM** dentro de esta ventana. Evidencia0600: `/tmp/psi-remote-config/cancel-retry-webhook-correlation-retry33m.json`. El proceso programado terminó; no queda polling activo ni se hizo una consulta adicional para recoger su resultado.

Root recuperó observación de Supabase mediante la sesión exclusiva de navegador ya autenticada cuando el llavero CLI dejó de exponer la credencial. La consulta SQL READ ONLY respondió201 y a las **20:48:35 UTC** confirmó **ACTIVE, un recibo y dos eventos**. La baja del proveedor sigue pendiente de persistirse mediante un reintento procesado por42VM; no se ejecutó recuperación manual ni POST local para suplir esa evidencia.

### Cierre de espera y recuperación administrativa de la cancelación

Se cerró la ventana automática a20:56:04UTC sin entregas nuevas en42VM. El coordinador ejecutó después una recuperación administrativa explícitamente separada, del **21:01:31.474 al21:01:33.986 UTC**: dos GET Mercado Pago200, vendedorTEST y recurso cancelado verificados, y una RPC **apply_subscription_webhook_event200 accepted=true**. Evidencia privada0600: `/tmp/psi-remote-config/cancel-recovery-smoke/result.json`. **No fue un webhook automático**.

El observador DBREADONLY posterior confirmó **CANCELED, un recibo, tres eventos y beneficiosfalse**; catálogo mensual intacto por ARS120.000. Con ese baseline, root inició la preparación de una suscripción nueva con UUID distinta para probar activación automática desde el inicio. Todavía no hay confirmación finalMP de ese nuevo recorrido. Release preparó lectura de logs acotada al deployment42VM y a la ventana que indicará el coordinador; no la ejecutó, no envió simulaciones ni hizo recuperación adicional. Los logs de runtime no incluyen data.id: la temporalidad debe complementarse con recurso y eventos DB del nuevo fixture, no basta un HTTP200 aislado para atribuirle el pago.

### Nuevo recorrido TEST: entrega automática200 sobre la versión corregida

El coordinador confirmó una sola vez el nuevo checkout TEST a las **21:14:32.377 UTC**; Mercado Pago mostró la confirmación a las **21:14:37.059 UTC**. Evidencia privada0600 `/tmp/psi-remote-config/repeat-mp-browser-confirm-once-20260912.json`. No se enviaron notificaciones sintéticas ni se ejecutó recuperación sobre este nuevo fixture.

La lectura de logs a **21:15:03.746 UTC**, desde21:14:30, confirmó **dos POST automáticos200** al webhook personal: **21:14:35.988** y **21:14:45.949 UTC**, ambos recibidos por el deployment corregido **dpl_42VMAv7sM5jC6wtHDggP6GDy5sPj**, dominio **universo-psi-mp-test.vercel.app**, sin errores internos. Evidencia0600 `/tmp/psi-remote-config/repeat-payment-webhook-correlation-after-confirmation.json`. El observador independiente del coordinador cruza ahora el recurso nuevo y los eventos/estado persistidos: los logs no exponen data.id, por lo que estos200 por sí solos todavía no demuestran activación del fixture concreto.

El cruce E2E posterior quedó **PASS**: observador READONLY a las **21:15:07 UTC** sobre la **nueva UUID** confirmó **ACTIVE, un recibo approved, dos eventos subscription_authorized_payment, is_demo=false y período pagado activo**, sin errores ni cambios de perfil/ranking. Las versiones del pago21:14:35/39 se procesaron a **21:14:38.549** y **21:14:46.337 UTC**, correlacionadas con las dos entregas automáticas200 del deployment42VM. Evidencia privada0600 `/tmp/psi-hosted-payment-after_repeat_payment_buyer-20260912.json`. El navegador QA respondió200 y mostró el dashboard activo; `/tmp/psi-app-buyer-browser-repeat-active-20260912.log`.

Por lo tanto, el nuevo recorrido **selección → checkout TEST → confirmación → webhook automático firmado → consulta autoritativa → persistencia ACTIVE → dashboard activo** está verificado. No hubo reconciliación administrativa ni evento sintético sobre esta UUID. Los dos eventos de versiones diferentes conservaron un único recibo. La cancelación automática de este nuevo recurso queda preparada por QA para verificar la corrección de lectura y cerrar la recurrencia de prueba; todavía no se ejecutó. Esta evidencia sandbox no autoriza cobros ni despliegue de producción.

### Nuevo recurso: cancelación y webhook automático200

La cancelación TEST del recurso nuevo fue aceptada entre **21:19:40.186 y21:19:42.198 UTC**: dos GET y un único PUTstatuscancelled200, identidad/condiciones/resumen cobrado intactos; init_point omitido como en el caso anterior. Evidencia privada0600 `/tmp/psi-remote-config/cancel-repeat-test-preapproval-once-evidence-20260912.json`. No se ejecutaron recuperación administrativa ni eventos sintéticos sobre esta UUID.

Release realizó **una sola lectura** de logs desde21:19:40, observada a las **21:20:28.725 UTC**. Confirmó POST automático al webhook personal a **21:19:46.664 UTC**, **HTTP200**, deployment corregido **dpl_42VMAv7sM5jC6wtHDggP6GDy5sPj**, dominioQA, sin errores internos. Evidencia0600 `/tmp/psi-remote-config/repeat-payment-webhook-correlation-followup.json`; su fase followup corresponde a **cancelación**, no a otro pago. Root cruza la lectura DB independiente para confirmar tópico, recurso y baja persistida; el log no expone data.id. No se realizaron consultas adicionales después de esta lectura.

El cruce de **cancelación automática quedó PASS**: observador READONLY a las **21:20:10 UTC** confirmó nueva UUID **CANCELED, período pagado no activo, un recibo exactamente conservado y cuatro eventos**, sin errores, is_demo=false. El evento subscription_preapproval de cancelación ocurrió a **21:19:42.003 UTC** y se procesó a **21:19:47.251 UTC**, correlacionado con POSTautomático200 de21:19:46.664 en42VM. El cuarto evento no es un pago duplicado: una autorización preapproval ocurrió a21:14:58.419 y se procesó a21:15:16.393, después del baseline inicial de pago; el historial contiene **dos versiones de pago, autorización y cancelación**. Evidencia0600 `/tmp/psi-hosted-payment-after_repeat_cancel_buyer-20260912.json`. NavegadorQA200, copia activa0/cancelada1.

Queda verificado el recorrido **alta → pagoTEST → activación automática → bajaTEST → cancelación automática**, con consulta autoritativa, firma, persistencia, historial y UI; sin reconciliación administrativa ni POSTsintético sobre la nuevaUUID. El primer recurso fallido y su recuperación manual permanecen documentados separadamente. No hubo cobros reales autorizados, cambios de precio ni activación productiva. Root preparó `.env.mercadopago.production.local` privado e ignorado con valores vacíos; el usuario tiene pendiente cargar credenciales reales fuera del chat. La identidad receptora y el webhook productivos aún no están verificados, por lo que no corresponde presentar la activación de producción como lista para aprobación completa.

### Ejecución productiva autorizada: configuración cerrada y primer build fallido

El usuario aprobó explícitamente «Sí, avanzá y dejá todo listo para recibir pagos reales», referido a VercelProduction, la única correctiva de Supabase y la apertura del mensualARS120.000 **después de los checks**, sin efectuar cargos reales ni cambiar precios. Root conserva el control de la migración y del paso de apertura.

Release preparó snapshot idéntico al QA aprobado **a68593b8d4d5e959ef65df607007674b14cebbed42b3d7a0cbb4c3c7ae0f46c6**,124fuentes/180archivos excluyendo secretos, en staging privado separado. Verificó proyecto original, dominio canónico, Supabasegwiz y SSO all_except_custom_domains. Aplicó nueve altas/actualizaciones de variables **exclusivamenteProduction**, usando stdin privado: cuentaactiva, environment, token, publickey, collector, webhooksecret, TEST_MODEfalse, SITE_NOINDEXfalse yCRON. El flag **MERCADOPAGO_CHECKOUT_ENABLED=false** existente enProduction+Preview se conservó sin modificar su alcance compartido; la base, sus claves, RATE_LIMIT_SALT y protección permanecieron intactos. Evidencia600 `/tmp/psi-remote-config/production-closed-env-applied.json`.

CLI59.15 almacena como Secret también variables nuevas sin --sensitive explícito: export privado comprueba flagfalse/base/dominio, pero devuelve [SENSITIVE] para modo,collector y flags nuevos. El intento de convertir cuatro valores no secretos con --typeconfig fue rechazado por laCLI, que exige eliminar/recrear; **no se eliminó ninguna variable ni se repitió esa operación**. Se conservan los valores literales aprobados enviados y su registro de éxito, sin inferirlos del placeholder. Se comprobó además en el código CLI que normalizeStdinEnvValue elimina el salto de línea final del stdin; no se presume un secreto con newline.

Deployment **dpl_DupeE1W4C7G2Ev2tJXX6DFPkpQ7k**, target **production**, terminó **ERROR**, aliasAssignedfalse. URL única **https://universo-i3f7p2t2r-nmarcosan-2648s-projects.vercel.app**. Logs a las **22:45:06 UTC**: «No se pudieron cargar las rutas de convenios» y «Failed to collect page data for /convenios/[slug]». Evidencia600 `/tmp/psi-remote-config/production-closed-build-failed.log`. **El dominio canónico conserva el deployment anterior; no hay nueva versióncompatibleREADY, migración ni apertura de checkout**. Se informó al coordinador para diagnosticar la consulta antes de reintentar; el buildlocal previo no demuestra la causa del fallo remoto.

Segundo intento productivo cerrado: root revisó la corrección limitada de lecturas públicas502/504, lint/typecheck/**253pruebas en12archivos** y buildproductivo con26GET200 aprobados. Snapshot **52851cacc0a313e547583275cfe0dd3a1085323309bf4ca2ccd4ed8adaf9b7a0**,126fuentes/182archivos; diff de fuentes limitado a public-fetch.ts, su prueba y public-repository.ts. Deployment **dpl_Bk7hK2FzexNmRgfLrDUiFf4nD5bi**, targetproduction, sin apertura de checkout. Compiló a las22:53:51 UTC y entró a Collecting page data a las22:54:00; a las23:00 UTC continuaba **BUILDING, sinerrorCode/errorMessage**. No se dio porREADY ni se cedió la migración. La ausencia de timeout propio en la lectura pública se informó como observación de código, no como causa probada del retraso.

Root recuperó por APIoficial de la sesiónSupabase exclusiva la **clave administrativa existente de gwiz**, sin crear ni rotar claves, y verificó GETsubscriptions200 frente a401 con publishable. Release actualizó únicamente SUPABASE_SECRET_KEY sensible enProduction a las **22:56:54 UTC**, para futuros deployments. Evidencias600 `/tmp/psi-remote-config/production-supabase-existing-key-verified.json` y `production-supabase-existing-env-updated.json`. No se asume que esa actualización cambie la configuración ya capturada por el builden curso. La apertura final incorporará explícitamente la clave verificada y mantendrá separado el flagProductiontrue del Previewfalse compartido, sólo después de la señal del coordinador.

El agente de aplicación reprodujo un bloqueo concreto: esperar `response.body.cancel()` sobre una rama clone/tee puede quedar pendiente hasta que se consuma la rama de caché. La corrección mínima y su regresión están en preparación. Por indicación del coordinador, release consultó el estado de **Bk7h** y, al confirmar BUILDING/aliasAssignedfalse, canceló **únicamente ese deployment** a las **23:01:58 UTC**, respuesta **CANCELED**. Evidencia600 `/tmp/psi-remote-config/production-public-retry-deployment-canceled.json`. La versión productiva servida no recibió alias nuevo; no hubo DDL ni apertura. Se detuvieron reintentos hasta completar los nuevos checks.

### Production compatible y cerrada: gate READY aprobado

La reparación clonefix dejó de esperar la cancelación de la rama clone/tee y agregó regresión; root aprobó lint, TypeScript, **254pruebas en12archivos** y buildproductivo con26GET200 a las23:02:17UTC. Snapshot **0b77dd621bd6c3bb829fde7293f84c2d659b8c887d793219f13951b3b7ba18dd**,126fuentes/182archivos, diff desde el intento anterior limitado a public-fetch.ts y su prueba; exclusiones de secretos verificadas.

Deployment **dpl_BETKKLQDNArnsoi5C8j5w9GYCmXs READY, targetproduction**, URL única **https://universo-ljsrtc2nh-nmarcosan-2648s-projects.vercel.app**. A las **23:05:29.518 UTC**, API de alias confirmó **https://universo-psi-eight.vercel.app** servido por ese ID. Export privado verificó **MERCADOPAGO_CHECKOUT_ENABLED=false**, base **gwizdgboqwpzyiaqcxbb** y SITEURLcanónico; las demás flags sensibles mantienen el valor literal aprobado. SSO all_except_custom_domains intacto. GET/planes **200** y GETwebhookpersonal **405**, ambos sin redirect. Este build ya incluye la clave administrativa existente de gwiz verificada y cargada antes de su creación. Evidencia600 `/tmp/psi-remote-config/production-closed-migration-gate.json`.

Se entregó el gate al coordinador para la **única correctiva autorizada**. En este punto la versión nueva compatible está servida con checkoutcerrado; release no aplicó SQL ni abrió cobros. Root prepara su gate normalizado y ejecutará migración/postflight antes de autorizar apertura. GET405 no equivale a firma validada; el smoke productivo firmado sin crear pago corresponde al tramo posterior.

### Apertura productiva aprobada y verificada, sin cargos de prueba reales

Root completó la única correctiva productiva **20260911224302**: envíoHTTP201 a las **23:06:52 UTC**, postflight **10/10 PASS a las23:07:08**, ocho controlesadvisorsPASS a las23:07:31 y GETPostgREST200 con las cuatro RPC nuevas expuestas. Se conservaron precio, datos e historial esperado y no se aplicaron las tres migraciones de catálogo. Evidencias600: `/tmp/psi-remote-config/production-migration-20260911224302.post.captured-20260912T230708Z.json`, `production-migration-20260911224302.advisors.captured-20260912T230731Z.json` y `production-postgrest-schema-verified.json`.

El smoke sin cargos sobre BET cerrado pasó cincoGET públicos, precioARS120.000, noindexfalse y sitemap23URLs, más **un probe firmado localmente de tipo desconocido200ignored a las23:08:19 UTC**. Ese probe verifica firma/configuración de producción, no es un evento comercial del proveedor y no creó pago. Evidencia `/tmp/psi-remote-config/production-nocharge-smoke-20260912/result.json`. Tras esos resultados, root autorizó la apertura prevista en la aprobación explícita del usuario.

Release separó el flag compartido sin abrir el Preview original: registro **O5YkqA2kuzZmhmZn** quedó exclusivamente **Preview=false**; creó registro **MIAJq7S7NZCMmBt3** exclusivamente **Production=true**. Ambos son configuración cifrada con visibilityconfig. Exports privados confirmaron valores y destinos; no se alteraron otros ambientes ni precios. Evidencia600 `/tmp/psi-remote-config/production-open-flag-result.json`.

Deployment final **dpl_Gp5xuyMB42J7cXZitzN5ozPpvaVq READY, targetproduction**, fuente idéntica **0b77dd621bd6c3bb829fde7293f84c2d659b8c887d793219f13951b3b7ba18dd**, URL única **https://universo-pds2kmmvp-nmarcosan-2648s-projects.vercel.app**. A las **23:12:44.955 UTC**, la API confirmó alias **https://universo-psi-eight.vercel.app** apuntando a ese ID. GatePASS: exportProductionCHECKOUTtrue, basegwiz y SITEURLcanónico, SSOintacto; **GETplanes200 conARS120.000, webhookGET405, robots200 indexable y sitemap200/23URLs**, sin redirects ni noindex. Evidencia600 `/tmp/psi-remote-config/production-open-release-verified.json`. No se envió otro POSTsintético después de la apertura ni se crearon recursos/pagos productivos.

**Estado: configurado y habilitado para recibir el mensual real aprobado**, con identidad real de vendedor/app, firma/configuración comprobada sin cargo y E2Esandbox automático de alta/bajaPASS. No se afirma que ya haya un cobro real procesado: la sesión no ejecutó ninguno. QA verifica el embudo público hasta login con todos los POST bloqueados. Recuperación compatible disponible: **dpl_BETKKLQDNArnsoi5C8j5w9GYCmXs**, mismas fuentes/RPC nuevas con checkoutfalse y claveSupabaseverificada. Ante incidente, cerrar únicamente el registroProduction y desplegar esa configuración compatible o usar rollback controlado aBET verificando todos los dominios; no volver al legacy ni alterar Preview. Esto frena nuevos checkouts, no cancela débitos ya autorizados externamente.

## Insumos externos y controles al cierre

1. Accesos, vendedor/compradorTEST, E2Esandbox, cuenta real63533235/app2533766701506518, URL/tópicos reales, secreto y aprobación del release: resueltos. No volver a pedir credenciales ni usar recursos heredados.
2. Correctiva productiva y postflight aprobados; configuración y apertura desplegadas. QA final público sinPOST en curso; la observación de un futuro pago productivo corresponderá a una transacción autorizada por su comprador, no a un cargo de prueba iniciado por esta sesión.
3. Mantener el procedimiento de reconciliación, la recuperación compatibleBET y las credenciales históricas. Un rollback del sitio no cancela recurrencias externas. Otros planesDRAFT permanecen fuera del alcance y sin nuevos precios.

## Configuración productiva revisada y aplicada bajo autorización

El usuario completó el archivo privado de credenciales reales. Root verificó GET/users/me **200 a las22:22:51 UTC**: collector **63533235**, siteMLA, **test_userfalse** y correo coincidente con el declarado por el usuario (sólo comparación booleana; correo fuera del reporte). GET de la aplicación **2533766701506518** en api.mercadolibre.com a las **22:27:53 UTC** confirmó nombre **Universo Psi**, activa, no bloqueada ni deshabilitada; el access_token devuelto coincidió con el local mediante comparación exclusiva en memoria. Eso vincula **app → token → cuenta real**. La respuesta no contiene owner_id: su ausencia no es un mismatch. Evidencias600: `/tmp/psi-remote-config/production-input-account-verified-20260912.json` y `production-application-token-link-readonly-20260912.json`. La publickey está presente, pero no se afirma validación independiente de su emparejamiento.

El webhooksecret real se cargó después enProduction bajo aprobación y se verificó mediante el probe firmado200ignored sin cargo sobreBET. URL/tópicos confirmados por el usuario. La aprobación explícita posterior permitió publicar, migrar y abrir el mensual; no autoriza iniciar cargos reales de prueba.

Destino exacto: Vercel **universo-psi / prj_PWqxPQDfHR3eJDVQXO9nAfa96vR3**, equipo **team_zbDtU9hfwTxtJvcj9sJMOp9u**, dominio **https://universo-psi-eight.vercel.app**; Supabase **gwizdgboqwpzyiaqcxbb**, organización **qwxmgztzoidyzkelupdr**. Artefacto probado QA: snapshot **a68593b8d4d5e959ef65df607007674b14cebbed42b3d7a0cbb4c3c7ae0f46c6**, deployment42VM. Es un hash de fuentes, no un commit nuevo: fijar el commit/artefacto definitivo antes de publicar y repetir checks sólo si cambia el código. Node24, framework Next.js explícito y build npm run build; metadatos actuales del proyecto reportan frameworknull, por lo que no asumir configuración de framework sólo por el vínculo local.

Producto limitado a **PROFESSIONAL_MONTHLY: ARS120.000, recurrente mensual, sin ciclos mínimos comprometidos, gracia3 días**, activo/PUBLISHED según preflight. Es la suscripción de profesionales a Universo Psi, no honorarios de consulta. Los otros tres planesDRAFT quedan fuera del alcance; no necesitan precio ni publicación. El mapeo preapproval_plan_id vacío permite el flujo recurrente directo ya probado. Confirmar cuenta/titular receptores y condiciones comerciales/fiscales; no inferirlos del prefijo de token ni de la aprobación de sandbox.

| Variable | Configuración aplicada a Production | Evidencia/alcance |
| --- | --- | --- |
| NEXT_PUBLIC_SITE_URL | https://universo-psi-eight.vercel.app | Conservar dominio canónico |
| NEXT_PUBLIC_SUPABASE_URL | https://gwizdgboqwpzyiaqcxbb.supabase.co | Conservar base productiva; nunca copiar sandbox |
| NEXT_PUBLIC_SUPABASE_PUBLISHABLE_KEY / SUPABASE_SECRET_KEY | Claves propias de gwiz, ya configuradas | Verificar ejecución; valores fuera del reporte |
| RATE_LIMIT_SALT | Propia de Production, ya presente protegida | Conservar, no copiar desdeQA |
| MERCADOPAGO_ACTIVE_ACCOUNT | personal, receptor real63533235 verificado | AplicadoProduction, protegido; firma/configuración resueltas enBET |
| MERCADOPAGO_PERSONAL_ENVIRONMENT | production | Aplicado bajo autorización; no se infiere del prefijo del token |
| MERCADOPAGO_PERSONAL_COLLECTOR_ID | 63533235 | Derivado de users/me y aplicadoProduction |
| MERCADOPAGO_PERSONAL_ACCESS_TOKEN / MERCADOPAGO_PERSONAL_PUBLIC_KEY | Credenciales reales de app2533766701506518 | Token vinculado por API con app/cuenta; publickey presente, sin comprobación independiente de pareja. AplicadasProduction bajo autorización |
| MERCADOPAGO_PERSONAL_WEBHOOK_SECRET | Secreto real protegido enProduction | URL/tópicos confirmados, probe firmado sin cargo200ignored enBET |
| MERCADOPAGO_CHECKOUT_ENABLED | true enProduction; false enPreview original | Registros separados y exports verificados; BET conserva snapshotfalse para recuperación |
| UNIVERSO_PSI_TEST_MODE | false explícito | AplicadoProduction; sin adaptadoresdemo |
| SITE_NOINDEX | false | Robots/sitemap/headers productivos comprobados indexables; QA conserva noindex |
| CRON_SECRET | Valor propio aleatorio de32bytes | Preparado privado600 y aplicadoProduction bajo autorización; corresponde al outbox, no a reconciliación de pagos |
| MERCADOPAGO_COMPANY_* | Ausentes mientras no haya cuenta company aprobada/histórica | No son requisito para habilitar únicamente personal |

Los nombres se tomaron de server.ts/.env.example. Las variables heredadas sin namespace de docs/deployment/vercel.md no son el contrato actual. NEXT_PUBLIC_MERCADOPAGO_PUBLIC_KEY no se requiere para el checkout recurrente por redirect actual. CRON_SECRET autentica el outbox existente; **no crea un cron de reconciliación de pagos**. El plan operativo debe conservar acceso de operador autorizado a la reconciliación administrativa sin inventar una cuenta SUPERADMIN.

**Webhook real a configurar en la app receptora:** `https://universo-psi-eight.vercel.app/api/webhooks/mercado-pago/personal`, tópicos **Planes y suscripciones** y **Pagos legacy**, secreto del ambiente real de esa aplicación. Retorno mensual exacto: `https://universo-psi-eight.vercel.app/dashboard?subscription=checkout-return`. El POSTpreapproval no configura notification_url por sí mismo; el webhook externo de la app es imprescindible. AuthSiteURL/callback deben corresponder al dominio canónico y no incorporar el PreviewQA en la base productiva.

Accesibilidad verificada por lectura a las **22:22:50 UTC**: Vercel conserva SSO **all_except_custom_domains**; GETsincredenciales al webhook canónico respondió **405 sin redirect**, mientras la URL única del deployment productivo respondió **302 protegido**. Evidencia600 `/tmp/psi-remote-config/production-webhook-access-readonly.json`. Usar el dominio canónico, sin bypass ni cambiosSSO. Este GET verifica llegada a la aplicación, **no un POST firmado ni procesamiento de pagos**; esas comprobaciones se harán únicamente en la etapa productiva autorizada. La [documentación de protección de Vercel](https://vercel.com/docs/deployment-protection) distingue dominios productivos de URLs generadas; los resultados efectivos del dominio exacto prevalecen sobre suponer acceso por esa regla general.

**Artefacto productivo revisado y ejecutado por el coordinador:** Datos entregó `/tmp/psi-remote-config/production-migration-20260911224302.REVIEW.md`, manifest, transacción, cuerpoAPI y verificador de sólo lectura, todos600. `scripts/payment-migration.mjs` conserva el bloqueo de producción: no se debilitó su guardia. La transacción separada aplica únicamente20260911224302 y registra su historial de forma atómica; SHA256 **e68da6b94cb340a3a098960763e01791fc3d443e4f714aa6b9c0f106d00e669f**. El cuerpo `.apply-request.json` tiene read_onlyfalse y es una futura escritura: **no enviarlo antes de aprobación y appcompatible/checkoutfalseREADY**. `verify.py` no tiene rutaapply y usa read_onlytrue. La envoltura productiva fue revisada y luego aplicada una única vez conHTTP201, postflight10/10PASS. El preflight se actualizó mediante la sesión exclusiva autenticada de Supabase y la API oficial antes de aplicar; el llavero CLI bloqueado no impidió completar esa comprobación. El hash revisado de la migración es **10b29b93ea002e68ab8657797b6a650b0346e53f1187792189c4051fcf105e19**. No utilizar dbpushgeneral: arrastraría las tres migraciones de catálogo pendientes del usuario. La [transición sandbox](payment-preflight-results-2026-09-12.md) ya verificó appcompatible/checkoutfalse → correctiva → postflight → no-opduplicado; no presentar ese ensayo como DDLproductivo ejecutado.

QA completó a las **22:31 UTC** el build de la rama de ejecución productiva en DockerNode24 con **VERCEL_ENV=production y SITE_NOINDEX=false**: veintiséis consultas públicas a gwiz respondieron200, sitemap con23URLs y noindex desactivado. Se usaron placeholders del servidor sin tokenMP: prueba el camino de build/publicación, **no autentica el secreto administrativo ni credencialesMP**. Root verificó en memoria que token, publickey y webhooksecret reales son distintos de sandbox; archivo600 y checkoutfalse. No se copiaron a Vercel. Este build sustituye el pendiente anterior de consultar el catálogo durante la compilación productiva; no demuestra que ya exista un deployment Production con esas fuentes.

**Configuración externa confirmada:** el usuario confirmó en la app real **2533766701506518 → Webhooks → Modo productivo** la URL canónica personal y los eventos **Planes y suscripciones + Pagos legacy** guardados. El secreto se desplegó bajo aprobación y su firma se comprobó enBET con un probe sintético sin cargo. No se conectó Chrome personal ni se pidió volver a revelar el secreto.

Orden propuesto para la autorización concreta: (1) credenciales/identidad/app/webhook y artefactos revisados, (2) publicar versióncompatible con checkoutfalse conservando originalsupabase, (3) aplicar correctiva dirigida y comprobarRPC/grants/RLS/historial/snapshot, (4) validar endpoint y reconciliación sin cargo real, (5) abrir checkout sólo con autorización de cobros y receptor confirmados. El camino de build productivo ya pasó las26 consultas públicas indicadas; la versión y credenciales efectivamente servidas se comprobarán después del deployment autorizado. Recuperación: conservar esta versióncompatible con checkoutfalse; **no volver al binario legacy después de eliminar firmasRPC**. Cerrar checkout no cancela suscripciones ya autorizadas; conservar credenciales históricas, webhook e historial y tramitar bajas sólo bajo la autorización correspondiente.

## Secuencia de release aprobada y ejecutada

Los pasos se ensayaron en el entorno aislado. Tras la aprobación explícita posterior del usuario se ejecutó la versióncompatiblecerrada, la correctiva dirigida y los checks; después se abrió Production con flag separado dePreview. La siguiente secuencia conserva el orden compatible del release y sirve de referencia operativa.

1. Integrar cambios de checkout/webhook y `20260911224302_harden_payment_persistence.sql`. No editar migraciones ya aplicadas ni usar `seed` en remoto. **La migración cambia firmas RPC y elimina las anteriores: no admite rollback a la aplicación vieja.** Ensayar una ventana breve en Preview aislado: publicar la aplicación nueva con `MERCADOPAGO_CHECKOUT_ENABLED=false`; hasta aplicar la migración, los eventos devuelven error reintentable, nunca éxito falso. Aplicar la migración y verificar/reconciliar recursos con la versión compatible antes de abrir checkouts. Comprobar también los errores transitorios y su recuperación. El ensayo sandbox de transición y postflight ya está documentado en payment-preflight-results-2026-09-12.md; el mecanismo dirigido productivo separado se ejecutó después del preflight vivo y BETREADYfalse; postflight aprobado.
2. Incorporar evidencia de `psi_qa_pagos`: lint, typecheck, unitarias, build, Playwright pertinente, contratos SQL e integración contra la base aislada. Separar fixtures de prueba del proveedor.
3. Con credenciales sandbox verificadas, ejecutar selección → checkout mensual → evento auténtico → consulta autoritativa → estado persistido y beneficios. Probar rechazo, evento adelantado, duplicado, concurrencia y cancelación; conservar identificadores técnicos y estado sin secretos/PII.
4. Registrar el deployment preview probado, commit final, historial de migraciones, vendedor sandbox, producto, moneda/importe y webhook validado. Preparar configuración productiva exacta y confirmar titular/condiciones.
5. Presentar esa evidencia y configuración al dueño para aprobar **activación de cobros en producción**. El recorrido sandbox automático ya está probado; la identidad receptora/app ya están verificadas; resta confirmar y probar configuración webhook real, completar preflight vigente y preparar la autorización concreta de despliegue/migración/apertura. Una prueba con cargo real requiere autorización adicional del cargo concreto.
6. Tras aprobación, repetir el orden ensayado: desplegar la aplicación compatible con checkout cerrado, comprobar la versión servida, aplicar la migración y validar RPC y reconciliación. Mantener las credenciales de cuentas históricas para procesar sus eventos. Recuperar las notificaciones que fallaron durante la ventana mediante reintentos y reconciliación administrativa; no depender exclusivamente de que el proveedor reintente. Abrir nuevos checkouts sólo después de esos checks, con la cuenta receptora aprobada. Si falla una etapa, conservar checkout cerrado y corregir hacia adelante. No cambiar credenciales o desplegar producción sólo para diagnosticar.

## Recuperación preparada

- **Frenar nuevos checkouts:** establecer `MERCADOPAGO_CHECKOUT_ENABLED=false` y desplegar esa configuración con la aplicación compatible, preservando el endpoint de webhook y la reconciliación de las cuentas históricas. No borrar credenciales como único mecanismo de cierre.
- **Suscripciones ya autorizadas:** pausar/cancelar en Mercado Pago sólo las suscripciones alcanzadas por la autorización del incidente, registrar los IDs y verificar el estado mediante API. Quitar variables, cambiar la cuenta activa o hacer rollback del sitio **no detiene los débitos recurrentes del proveedor**.
- **Aplicación:** conservar **dpl_BETKKLQDNArnsoi5C8j5w9GYCmXs**, probado con las RPC nuevas y `MERCADOPAGO_CHECKOUT_ENABLED=false`. Ése es el destino compatible de recuperación; verificar flag y dominios tras cualquier rollback. No volver a `de0fbef` ni a la versión legacy que usa firmas eliminadas. Si BET no estuviera disponible, publicar la fuente actual con checkout cerrado y corregir hacia adelante.
- **Datos:** corregir hacia adelante, conservar snapshots, pagos, eventos y auditoría; no borrar historial ni devolver estados pagados por el mero retorno del navegador.
- **Continuidad:** mantener credenciales y firmas de las cuentas con suscripciones vigentes; revisar eventos pendientes y recursos del proveedor después de recuperar. Cerrar el incidente sólo tras confirmar reconciliación e historial persistido.
- **Notificación perdida:** usar la acción de reconciliación en `/admin/suscripciones`; recupera preapproval e historial paginado de facturas/pagos y vuelve a validar los recursos. No hay cron de reconciliación de pagos habilitado. Un historial superior a 1.000 recursos devuelve un error explícito y requiere tratamiento operativo por lotes.
- **Checkout de resultado incierto:** la reserva impide repetir un POST que podría haber creado una autorización. Reintentar recupera un recurso único por referencia externa (o ID de preferencia guardado). Si no aparece ningún recurso, conservar la reserva hasta revisar la cuenta histórica; no resetearla ni volver a cobrar automáticamente. La búsqueda de preferencias sin ID está limitada por Mercado Pago a los últimos 90 días.

## Resultado por ambiente al cerrar este inventario

| Ambiente | Resultado |
| --- | --- |
| Local | Correcciones integradas; stack propio `psi_payments_20260911`, API `http://127.0.0.1:55321`. Build con Node 24 dentro de Docker, unitarias e integración verificadas; detalle final en el informe QA. El stack previo 54321 permaneció sin cambios. |
| Sandbox del proveedor | E2E automático de alta/pago/activación/cancelación PASS con vendedor test_user/MLA, comprador autenticado, tarjeta oficialTEST y ARS120.000 sin cambios. Nueva UUID final CANCELED/beneficiosfalse, un recibo conservado y cuatro eventos sin errores/is_demo=false; dashboard cancelado. Ninguna recuperación o simulación sobre esaUUID. Primer recurso cancelado con recuperación administrativa separada. App2796628634374866 y firma verificadas. |
| Preview protegido | Deployment anterior disponible bajo SSO y checkout cerrado. El enlace temporal de revisión fue revocado; permanece como evidencia histórica. El recorrido de pagos usa ahora el proyecto público exclusivo QA, sin cambiar la protección del original. |
| Nuevo Preview público QA | universo-psi-mp-test, Hobby, sin Git, base exclusiva gzsbndvaxdyuyhjidfqq; quince variables sólo Preview, CHECKOUT=true sóloQA, TEST_MODE=false y SITE_NOINDEX=true. SHA a68593b8d4d5e959ef65df607007674b14cebbed42b3d7a0cbb4c3c7ae0f46c6; dpl_42VMAv7sM5jC6wtHDggP6GDy5sPj READY targetPreview. Activación y baja automáticas nuevaUUID PASS; recibo/historial conservados, perfilDRAFT/ranking intactos. Correcciones payment recurrente, canceled/cancelled, init_point nullable y buildQA integradas; lint/typecheck/240tests/buildNode24 aprobados. |
| Development Vercel | Sin variables de aplicación al inspeccionar; descarga automática apartada del repo. |
| Production | **Habilitado para recibir PROFESSIONAL_MONTHLY120000ARS/mes** bajo aprobación explícita. Gp5xuyMB42J7cXZitzN5ozPpvaVq READY targetproduction y aliascanónico verificados23:12:44UTC, fuente0b77dd621bd6c3bb829fde7293f84c2d659b8c887d793219f13951b3b7ba18dd. Correctivaúnica aplicada/postflight10/10+advisors8PASS, cuenta/app reales verificadas, firma200ignored sin cargo, CHECKOUTtrue exclusivoProduction/Previewfalse, Supabasegwiz y SSOconservados. Planes200/preciointacto, robotsindexable/sitemap23; QAfinalpúblico en curso. RecuperaciónBETcompatible/cerrada. No hubo cargos reales. |

Este estado actualizado prevalece sobre afirmaciones históricas contradictorias como «ninguna credencial», «Vercel inaccesible», «destino Supabase desconocido», «Management Supabase 403/login pendiente» o «sin cambios remotos». Los vínculos locales de Vercel y Supabase ya apuntan a los proyectos correctos. La presencia de las variables indicadas fue comprobada; la identidad y ambiente del vendedor sandbox sí fueron verificados por /users/me; el pago sandbox y la recuperación administrativa están verificados; la simulación emitida desde Mercado Pago validó conectividad/firma; el nuevo recorrido ya probó activación y cancelación automáticas íntegras; la configuración productiva fue aprobada y desplegada, con apertura verificada sin ejecutar cargos reales. No se concluye que existan cobros activos por la mera configuración.


## Antecedente histórico: preparación de la aprobación a las22:34 UTC

Este registro conserva el estado anterior a la aprobación y ejecución descritas arriba; ya no representa pendientes actuales. El dueño confirmó explícitamente que la aplicación real2533766701506518 tiene guardados el endpoint canónico personal y los eventos Planes y suscripciones + Pagos legacy. El secreto real está cargado localmente; la firma productiva todavía se comprobará durante el release autorizado.

Preflight actualizado mediante SQL Editor exclusivo de producción, BEGIN READ ONLY:9/9 comprobaciones PASS, ocho versiones legacy, seis firmas anteriores, ninguna nueva, cero registros financieros y mensual120000ARS intacto. Observación22:34:33UTC; respuestaHTTP no capturada por timeout del listener, resultado obtenido de una fila visible en la UI. Se validaron offline los hashes de la correctiva y los cuatro artefactos del manifest. Evidencia privada600 `/tmp/psi-remote-config/production-migration-20260911224302.pre.ui-20260912T223433Z.json`. No se aplicó DDL; repetir preflight antes de ejecutar.

CRON_SECRET aleatorio de32bytes, UNIVERSO_PSI_TEST_MODE=false y SITE_NOINDEX=false preparados en `/tmp/psi-remote-config/production-runtime.proposed.env`,600, únicamente local. Cron sigue correspondiendo al outbox, no a reconciliación de pagos.

Propuesta lista para revisión del dueño: cargar configuración sólo en Production de Verceluniverso-psi, desplegar versión compatible con checkout cerrado, aplicar únicamente20260911224302 sobre gwiz, verificar contratos/permisos y smoke sin cargo, y abrir el mensualARS120000 sólo con aprobación y controles satisfactorios. Si falla algún control, conservar checkout cerrado y corregir hacia adelante. No incluye cargos reales de prueba, cambios de precio ni migraciones de catálogo. Ninguna publicación, migración o apertura productiva se ejecutó con la mera confirmación de carga/configuración.


## Checkout con tarjeta en el sitio — en validación, 13 de septiembre UTC

El nuevo recorrido abre `/dashboard/suscripcion/pagar?subscriptionId=…` y carga MercadoPago.js CardForm con campos de tarjeta en iframes. El correo del pagador es independiente del correo de acceso; el servidor recibe token efímero, correo y consentimiento, nunca PAN/CVV. La autorización usa el precio del snapshot y no activa beneficios hasta confirmar el pago. Reservas privadas con identificador de intento permiten reintentar sólo ante rechazo definitivo 400/422; respuestas ambiguas conservan la reserva. La migración `20260913012905_add_card_checkout_attempt_leases.sql` agrega ese contrato sin modificar precios ni migraciones anteriores.

Pruebas locales: 326 unitarias PASS; lint/typecheck y build Node24 PASS; SQL y tres carreras de concurrencia PASS. El recorrido sandbox auténtico permanece pendiente en este registro. No confundir esta preparación con un cobro real ni con despliegue del nuevo checkout en producción. La recuperación de un checkout alojado anterior exige confirmación del usuario y estado pendiente sin facturación; la API de Mercado Pago no documenta cancelación condicional atómica frente a una autorización concurrente en otra pestaña.


La migración de reservas se aplicó de forma dirigida a sandbox y producción, con postflight conforme en ambos. Producción conserva once versiones revisadas y cero intentos al aplicar; no se crearon recursos de Mercado Pago ni se publicó todavía el checkout embebido. Evidencia local de producción: `/tmp/psi-supabase-browser-card-lease-prod-postflight-20260912.log`.


Preparación de release TEST del checkout embebido (13/09, 01:44 UTC): se revalidó Vercel `prj_nZDkEEpIowd5rSJbYBWfyhQuVZDl`, equipo existente, Node24 y acceso público limitado al proyecto QA. Las quince variables permanecen exclusivamente en Preview y sin cambios: base `gzsbndvaxdyuyhjidfqq`, cuenta `personal`, ambiente `sandbox`, collector3170200305/appTEST2796628634374866, checkout habilitado, fixtures deshabilitados y noindex. La clave pública se entrega al CardForm desde `MERCADOPAGO_PERSONAL_PUBLIC_KEY`; no se agregó una variable genérica. Archivos privados600 y exclusión de secretos del upload verificados.

Datos confirmó la lease sandbox aplicada HTTP201 y postflight9/9 PASS a01:41:48UTC; historial10versiones, cero intentos iniciales y suscripciones/eventos previos conservados. La migración de presentación incompleta no está en sandbox: el fixture QA debe conservar presentación válida. Deployment `dpl_4M1WKKLWeg4WjfQLbva5rG7X8ab2` iniciado como Preview (target omitido en API, respuesta null), SHA256 `2d2dceec7da4e6540f03125d832165206c2147deba71b06eda32a6499c79ecf9`,142fuentes/198archivos. El alias sólo cambia tras READY; el E2E del checkout embebido continúa pendiente. Este release no modifica variables ni deployment de producción real.

Preview embebido READY y servido: `dpl_4M1WKKLWeg4WjfQLbva5rG7X8ab2`, target Preview, alias `https://universo-psi-mp-test.vercel.app` verificado por API a01:47:51UTC. GET `/planes`200 y GET webhook405, ambos noindex; esas lecturas no crean recursos ni prueban la entrega del proveedor. Se comprobó igualdad byte a byte de las142fuentes con el workspace del build aprobado. QA recibió el gate para continuar con una identidad ficticia nueva y el recorrido auténtico; todavía no se atribuye éxito E2E al nuevo checkout embebido. Evidencia privada600: `embedded-qa-release-verified.json`, `embedded-qa-deployment-latest.json` y `test-preview-current-source-evidence.json` en `/tmp/psi-remote-config`. Para recuperar sólo la aplicación QA, conservar el deployment previo42VM y la lease aditiva; frenar nuevos checkouts no cancela autorizaciones ya existentes ni debe borrar historial.

### Validación completa del checkout integrado — 13 de septiembre, 02:29 UTC

El intento TEST nuevo en `dpl_GcHKUgNZefyaBgjnxUftLce2WD6h` tokenizó la tarjeta (201) y autorizó la suscripción sin salir del sitio (POST 200 a las02:18:45UTC). Correo de comprador TEST diferente del correo de acceso, seller TEST3170200305 verificado y mensual120000ARS intacto. Las lecturas autenticadas constataron pago approved/accredited, importe y referencias coincidentes; `live_mode=true` es el comportamiento de esta cadena recurrente de cuenta TEST previamente soportado, no una operación con dinero real.

Los primeros eventos de pago respondieron502; un reintento auténtico de Mercado Pago a las02:19:47.333UTC obtuvo200. El evento se procesó a las02:19:47.831709UTC. La lectura final y el dashboard acreditan ACTIVE, un recibo y dos eventos, sin simulación ni reconciliación manual persistida. La investigación SQL usó service_role y rollback completo, validó la RPC y no produjo cambios. No se atribuye el error inicial a la base: los recursos actuales y la función pasan, y el reintento automático recuperó el pago.

La versión agrega diagnóstico por fase y códigos permitidos para distinguir fallas de lectura del proveedor y persistencia sin guardar datos de tarjeta ni errores privados. QA final `dpl_7QSFWnVPeKnhu855Fq9T7qRNaf2P`, mismo procesamiento con ese diagnóstico adicional. FuenteSHA256 `ffa4b33e992fd6eac102a68644f972c5323ba77279e0e09c011aca2a99d6b955`: lint/typecheck,338 tests y build Node24 PASS. Build con26 lecturas públicas200 y ninguna operación financiera. Release productivo `dpl_DPruWEU3WnRqXN5hszQeqkgTSqTN` solicitado; pendiente verificar READY y alias en el siguiente registro.

Release confirmado02:31UTC: `dpl_DPruWEU3WnRqXN5hszQeqkgTSqTN` READY, target production y alias `https://universo-psi-eight.vercel.app` coincidente. Smoke público: registro/ingreso/planes200, mensual120000ARS visible, checkout anónimo307 con `next` preservado hacia ingreso y webhookGET405 esperado (endpointPOST). Todas las respuestas con CSP. Cero cargos reales, cero cambios de precios y ninguna cuenta productiva creada en el smoke.

Limpieza TEST verificada02:32UTC: cancelación única del preapproval nuevo, confirmada en Mercado Pago; webhook auténtico200 a las02:31:25.866UTC y evento procesado02:31:26.85849UTC. Estado final CANCELED, un recibo y tres eventos conservados, sin borrados ni reembolsos ni reconciliación manual. Las otras suscripciones no se modificaron.
